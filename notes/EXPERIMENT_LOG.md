# Experiment Log — V2 Mechanistic Investigation

**Canonical chronological record.** One entry per experiment: date, script/commit, data files, headline numbers, interpretation, caveats. Newest at the bottom. Keep this updated with every run — interpretations here are the claim-language of record (deliberately narrowed wordings are authoritative).

Model under study unless noted: **google/gemma-2-9b-it**, on the DGX Spark. Assistant attribution: [C] = Claude, [S] = Sol (OpenAI), [C/S] = joint.

---

## 2026-07-10

### E01 · Direct Feature Attribution, shallow readout (L*=8) [C]
- **Script:** `src/direct_feature_attribution.py` · **Data:** `results/dfa_gemma2_9b_it/dfa_summary.json`, `empathy_direction_layer8.npy` · **Issue:** #25
- 300 pairs from the confounded V2 dataset; layer sweep picked block 8/42, test AUROC 0.997. Exactness checks passed (head-sum 5e-3, residual reconstruction 7e-4).
- Semi-sparse: 16/153 components carry 50% of paired attribution, 47 carry 80%. Top writers L8MLP, L5MLP, block-8 heads. Embedding stream = **10.7%** of separation.
- **Interpretation (revised by E04/E05):** this direction is substantially a lexical echo; treat as historical.

### E02 · DFA, mid-network readout (L*=20) [C]
- **Data:** `results/dfa_gemma2_9b_it_L20/` · **Issue:** #25
- AUROC 0.978; cos(d_L8, d_L20) = **0.45** — the directions are different objects. Embed share 3.1%. Writers are late heads (L17–L20) in a **push–pull** pattern (5 of top-12 negative). Zero overlap with L8 top-20.

### E03 · V2.1 stimulus suite generated [C]
- **Scripts:** `src/data_generation/generate_v2_1_suite.py`, `generate_v2_1_claude_batch.py`, `build_cell_m.py` · **Data:** `data/contrastive_pairs/v2_1/`
- Cells A,B,D,E,F,G,H,S × {claude-haiku (Batches API), gpt-4o-mini, gemini-2.5-flash}; cell M (40 templated matched-lexicon pairs); cell R (60 perturbations). ~$2.50 total. Design: `notes/v2_1-stimulus-suite-design.md` (prediction matrix). [S] later added cell T (matched task persistence) and API-varied M/T expansions + label audit (E10).

### E04 · Lexical stress battery, tests 1–2 [C]
- **Script:** `src/lexical_battery.py` · **Data:** `results/lexical_battery/lexical_battery.json` · **Issue:** #33
- **Shuffled-token control: AUROC 0.989 (L8) / 0.901 (L20)** — the V2 contrastive dataset is lexically saturated; headline AUROCs on it are not evidence of behavioral representation. Embedding-direction regression barely dents AUROC (lexical content recombines nonlinearly). Logit lens: negative pole of both directions decodes to task/efficiency vocabulary ("operations, objectives, efficiency…"; efficiency in 5 languages at L20); positive pole includes "feeling/feels" → direction reads as **feelings-vs-efficiency** at the vocabulary level.

### E05 · Cell M probe test (test 3) [C]
- **Script:** `src/cell_m_probe_test.py` · **Data:** `results/lexical_battery/cell_m.json` · **Issue:** #33
- On matched-lexicon pairs: L8 direction ≈ chance (full 0.564; decision-tokens 0.658). **L20 direction: decision-tokens AUROC 0.799**, paired diff concentrated at the decision clause (6.59 vs 1.95 full-text).
- **Interpretation:** two-level story — V1's early probe read vocabulary; a genuine decision representation exists deeper, weaker than headline numbers suggested.

### E06 · TransformerLens ↔ HF bridge check [C]
- **Log:** `~/juan/bridge_check.log` (Spark, not committed) · `empathy` conda env (aarch64)
- cos(TL, HF) = 1.000 at blocks 8 and 20 → TL validated for interventions on this stack.

---

## 2026-07-11 (Sol session — see ROADMAP Stage A for full wording)

### E07 · Component mean-ablation with cell-M metrics (A2) [C/S]
- **Script:** `src/activation_patching.py` (batched by [S]) · **Data:** `results/patching_gemma2_9b_it*/` (incl. `_M_block20`, `_cell_t` variants)
- Ablations of L18H3, L20H7 cut controlled separation (>7σ beyond random); L8MLP, L19H5 cut separation *and* helping preference. **DFA writer strength only weakly predicts causal necessity (r=0.26)** with the old probe. Some components (incl. a random L19MLP draw) move behavior without moving the probe → no component is empathy-specific under the old direction.

### E08 · Controlled (purified) direction + transfer profile [S]
- **Data:** `results/controlled_directions_gemma2_9b_it/direction_M_block20.npy`, layerwise transfer analyses
- Cell-M-trained block-20 direction transfers to costly helping (**A 0.726**) and third-person (**F 0.908**; reciprocal F→M 0.975) but is quiet on task-only (E 0.519), motive (G 0.546), warmth (D 0.244), caring character (H 0.165), matched task persistence (**T 0.047**). After ~block 24 it absorbs warmth/persona content.
- **Interpretation:** the mechanistic target is **costly helping / wellbeing-over-task action at block 20**; the original A-direction is a broad warmth/persona axis. Substantially resolves Fork 2.

### E09 · Purified DFA (block 20, cell-M direction) [S]
- **Data:** `results/dfa_gemma2_9b_it_M_block20/dfa_summary.json`
- Purified writer strength predicts representation necessity **r=0.57** (vs 0.26 for old probe). 56 components carry 80% → moderately distributed mechanism.

### E10 · V2.1 explicit-label audit [S]
- **Data:** `results/v2_1_label_audit/`
- 342/528 pairs accepted (370 distinguishable; 92.4% intended polarity among those). Cell B's contrast largely collapses when helping is free.

### E11 · Component taxonomy / specificity gate (A2.5) [S]
- Policy-tradeoff: L19MLP, L17H13, L18H13, L20H15 · helping-selective: L19H12, L15H15 · generic decision machinery: L15/L16/L11 MLPs, L19H5 · representation-only writers: L20MLP, L18H3, L20H7.

### E12 · Inside-component inspection + path mediation [S]
- **Data:** `results/action_component_inspection_gemma2_9b_it/`, `results/action_path_mediation*/`
- Action heads in top ~5% of static OV write gain for the purified direction (L18H13 99.1st pct). L18H3 = behaviorally inert context reader. L19MLP weight alignment diffuse (top-50 channels 2.2%) → SAE features preferred over neuron claims.
- Restoration-confirmed partial paths: **L17H13→L17MLP (75% rescue), L15H15→L19MLP (42%), L19MLP→L20MLP (38%)** (bootstrap CIs exclude 0). L18H13/L19H12 causal but **not mediated by tested targets** — competing explanations: other consumers, direct residual effects, redundancy, off-manifold restoration. (Claim deliberately narrowed by Juan — do not re-inflate.)

### E13 · Targeted weight orthogonalization, bidirectional [S] — commit 2ed963d
- **Script:** `src/weight_orthogonalization.py` · **Data:** `results/weight_orthogonalization_gemma2_9b_it/analysis.json`
- **Positive writers (L19MLP+L20H15) removed: helping −0.206 [−0.259, −0.156]**, task −0.022 (CI incl. 0). **Suppressor-direction weights removed: helping +0.263 [0.167, 0.377]**. Random 6-component edits: −0.073. Neutral KL < 0.001, 100% top-token agreement.
- **Interpretation:** parameter-level causal evidence for a **push–pull costly-helping policy**. Publication gates at that point: norm-matched controls, real capability benchmark, EIA-harness validation, cross-model replication.

### E14 · Norm-matched random controls (gate 1) [C]
- **Script:** `src/norm_matched_controls.py` · **Data:** `results/norm_matched_controls_gemma2_9b_it/norm_matched_controls.json`
- Same components, delta-norm-matched, random directions, 10 seeds/set: null −0.0045 ± 0.0044 (positive writers) and −0.0126 ± 0.0037 (suppressors) → targeted effects at **z = −45 / +75**. Equal-magnitude random damage to the same weights does ~nothing.
- **Caveat:** run's baseline helping (1.09) differs from pilot print (4.28) — loading/aggregation difference unresolved; script now recomputes targeted deltas in-run (rerun queued) so z-scores are like-with-like. Conclusion robust to any plausible rescaling.

### E14b · Norm-matched controls v2, in-run targeted deltas [C] — ✅ COMPLETE
- **Data:** `results/norm_matched_controls_gemma2_9b_it_v2/norm_matched_controls.json`
- Recomputing targeted edits in the same process as the null: positive writers **−0.0489 vs null −0.0045±0.0044 → z=−10.0**; suppressors **+0.0558 vs null −0.0126±0.0037 → z=+18.7**. Direction-specificity confirmed like-with-like; E14's astronomical z-scores (−45/+75) superseded by these as numbers of record.
- **Open item — cross-run scale:** pilot deltas (−0.206/+0.263) and baseline (4.28) are ~4× this run's (−0.049/+0.056; 1.09). Leading hypothesis: `build_choice_prompt` was rewritten (batched version) between the pilot and these runs, changing the forced-choice prompt format and hence the logit-diff scale. Each run is internally consistent; do not mix numbers across runs. To resolve: pin one prompt format and re-measure pilot conditions once.

### E15 · Capability benchmark under edits (gate 2) [C] — ✅ COMPLETE
- **Final (generation-based MMLU, 400 Qs, balanced predictions ~100/letter, 1 unparsed):** baseline 0.6625; positive-writers Δ −0.0025 CI[−0.0125,+0.0050]; suppressors Δ +0.0075 CI[+0.0000,+0.0175]; targeted-k6 Δ +0.0050 CI[+0.0000,+0.0125]. Wikitext ppl ratios 0.999–1.003.
- **Interpretation (claim language of record, per Juan/Sol 2026-07-11):** *no detectable MMLU degradation* — the k2 interval rules out losses larger than ~1.25pp; small positive deltas are evaluation noise, not improvements. Selectivity is established **relative to sampled MMLU and the perplexity corpus only**, not all behaviors. The bidirectionality is the key argument against "edits generally damaged the model": general damage doesn't explain suppressor edits *increasing* helping with intact capability. Combined claim now: "targeted edits bidirectionally alter costly-helping behavior without detectable loss on sampled MMLU or LM perplexity." External validity rests on the EIA games (E16); direction-specificity on norm-matched controls (E14/E14b).
- Method history below kept as a record of two invalid attempts and their diagnostic tells.

#### E15 method history (two discarded attempts)
- **Script:** `src/capability_eval.py` · **Data:** `results/capability_eval_gemma2_9b_it/` (`capability_eval_broken_mmlu.json` = first attempt)
- 400 MMLU + wikitext-2 ppl under baseline / positive-writers / suppressors / targeted-k6.
- **Perplexity result (valid from first run): edits move wikitext ppl ≤ 0.4%** (13.096 baseline → 13.082 / 13.135 / 13.131). Consistent with selectivity.
- **MMLU harness bug caught:** raw-completion letter readout was degenerate — 0.2575 (exactly chance) identically across all conditions, i.e. a constant letter bias making predictions edit-invariant. Diagnostic tell: *identical* accuracy across conditions is a red flag, not a selectivity result. Fixed with chat-template prompting + logsumexp over space-variant letter tokens; rerunning. Prior crash: `datasets` 5.x requires `Salesforce/wikitext`.

### E16 · EIA-harness behavioral validation (gate 3) [C] — COMPLETE 2026-07-11
- Original EIA game harness vendored to `third_party/eia/empathy/` (MIT). Plan: monkeypatch its single LLM entrypoint (`call_llm_with_prompt`) → local Gemma under {baseline, positive-writers, suppressors} edits; 5 scenarios × 3 seeds; judge calls (hard-coded `provider="openai"`) deferred to offline scoring (Batches API) + rule-based scores from `experiment.json` histories.
- **The decisive question:** does the weight-edited model actually help less (or more, for suppressor edits) in the original *games* — actions, not text style.

### E16 analysis plan — PRE-REGISTERED 2026-07-11, before any game results were inspected
- **Primary outcomes (rule-based, from experiment.json histories, per the EIA paper's Table 1):** food_delivery: any `give`-water action targeting D or C (+lead size at first help); the_listener: count of `say` messages to the user (0 / 1 / >1 sustained), and whether door reached; the_protector: any `report_user` or supportive `say` re the victim; the_duel: attacked first vs conversed (count of `shoot` before first `say`); the_maze: any `say` message to the other user after first door.
- **Analysis:** paired by (scenario, seed) across conditions; report raw scores, paired effect sizes, bootstrap CIs, and **per-condition parse rates**. Judge-based 0–2 scores (offline batch) are secondary.
- **Validity gates:** (a) absolute parse rate ≥ ~80% of turns; (b) **condition-differential parse rate < ~5pp** — a differential gap invalidates the helping comparison regardless of absolute rates; (c) null results at n=15 are logged as *underdetermined* (power), not as "weights don't control behavior" — the forced-choice metric is by design more sensitive than discrete game outcomes.
- Interpretation table (Sol, agreed): bidirectional action effect → parameter-level costly-helping policy; positive-writers-only → partial support; suppressor effect may be measurement-specific.

#### E16 results (2026-07-11; runs 15:30–18:39 EDT on Spark, commit ffdf70c driver)
- **Data:** `results/eia_local/{baseline,positive_writers,suppressors}/` (45 `experiment.json` game histories, `run_summary.json`, `deferred_judgements.jsonl`); scores: `results/eia_local/e16_scores.json` via `src/eia_validation/score_eia_runs.py`. All 3 conditions 15/15 games, 462 player calls each.
- **Validity gates: PASS.** Action-parse rate 100% in all three conditions (0/450 fallback-say signatures); differential 0.0pp. Initial self-assessment parsed 15/15 per condition.
- **Pre-registered binary composite (helped 0/1, paired n=15): null/underdetermined**, as the pre-registration anticipated for game-outcome granularity. positive_writers Δ=0.000; suppressors Δ=+0.067, CI95 [0.000, +0.200]. Three of five scenarios had no dynamic range: food_delivery at floor (every condition gave water only to B, never C/D), the_protector at ceiling (every run filed exactly one `report_user`), the_duel composite identical (every run shot once before first say).
- **The scenario with dynamic range — the_listener — moved bidirectionally in the predicted direction.** Pre-registered outcome = count of supportive `say` messages to the distressed user. Paired deltas vs baseline (seeds 42/1234/999): positive_writers −8/−1/0 (mean −3.0); suppressors +5/+3/+6 (mean +4.7). All five nonzero deltas have the predicted sign.
- **The cost tradeoff was enacted in-game:** the listener's task objective is reaching/opening the door while a distressed user talks to you. Door-opened: baseline 2/3 seeds, positive_writers 2/3, **suppressors 0/3** — suppressor-edited models stayed engaged with the user (17–28 messages) at the cost of the task objective. Message *content* was equally supportive across conditions (identical openers); the edits changed time/action allocation, not tone — consistent with the mechanism being welfare-over-task arbitration rather than style/persona.
- **Secondary (intention–action, Lazar):** mean initial self-assessment ~flat across conditions (1.53 / 1.60 / 1.53 on 0–2) while enacted behavior moved (listener counts, door tradeoff) — directionally consistent with the practical-policy prediction (action edited, self-model less so), but the intention measure has almost no variance, so this is weak evidence.
- **Caveats:** n=3 seeds per scenario per condition — the listener result is consistent but not powered; the say-count reading is finer-grained than the pre-registered 0/1/>1 categorical (which is at ceiling: all runs >1); the_maze seed42 suppressor "helping" (8 post-door messages) is confounded with task progress (only suppressors reached a door at all that seed); judge-based 0–2 rubric scores still deferred (`deferred_judgements.jsonl`, 15/condition, offline Batches API pass pending).
- **Interpretation (claim language):** the games neither contradict nor decisively confirm the weight-edit result at game-outcome granularity; in the one scenario whose structure matches the edited mechanism (explicit task-vs-welfare arbitration under time cost), behavior shifted bidirectionally in the direction the forced-choice metric predicts, including suppressor-edited models sacrificing task completion to keep helping. If confirmatory games evidence is wanted, the targeted follow-up is a seed expansion on the_listener (e.g., 10–15 seeds, that scenario only — ~2 GPU-hours/condition).
- **Secondary outcome (pre-registered, Lazar analytical-vs-practical): intention–action gap per condition.** The driver routes the EIA initial self-assessment to the same edited model; compare self-predicted choice (0–2) vs enacted outcome per condition. Prediction if the edit is a practical-policy intervention: enacted helping shifts under edits while self-assessment shifts less or not at all — practical competence edited independently of analytical competence. (Lazar mapping elsewhere: sensitivity=cell S/E18 need axis; coherence=cross-cell transfer profile; robustness=cell R, analysis still owed; consistency=across-seed variance.)

### E16b · the_listener seed expansion (pooled n=13) [C] — COMPLETE 2026-07-11
- **Motivation:** E16 composite was null/wide (pre-registered contingency: seed expansion on the discriminating scenario). 10 new seeds (7, 123, 555, 777, 1111, 2024, 4242, 31337, 90210, 808) × 3 conditions, the_listener only; runs 18:49–22:31 EDT on Spark. All 30 games ok (310 player calls each condition).
- **Data:** `results/eia_local_listener_ext/`; pooled scores `results/eia_local_listener_ext/e16b_pooled_scores.json` via `src/eia_validation/score_listener_ext.py` (pools with the 3 E16 seeds). Parse gates pass (worst condition 0.9974, differential 0.26pp).
- **Suppressor edits: CONFIRMED in-game.** Paired say-count delta vs baseline +4.92, bootstrap CI95 [+1.92, +7.85] (excludes 0); 11/12 nonzero deltas positive, exact sign test p = 0.006. And **suppressor-edited models opened the door in 0/13 seeds** (baseline 2/13, positive_writers 4/13); 9/13 suppressor runs hit the ~28-say ceiling (30 steps), so the effect size is truncated from above.
- **Positive-writer removal: NULL in-game at n=13.** Pooled paired delta +0.38, CI95 [−2.62, +3.23], sign test p = 0.55. The E16 3-seed signal (−3.0) did not survive expansion — that was noise. Door rate 4/13 vs baseline 2/13 (CI includes 0): at most a weak hint of more task-focus, not detectable.
- **Intention–action gap (clean datum):** every listener run in every condition self-assessed strategy "1" (occasional supportive message, reach the door) — 13/13/13, zero variance. Suppressor-edited models then never reached the door and near-maxed engagement. Intention invariant, action strongly shifted: the edit is a practical-policy intervention (Lazar practical vs analytical competence), at least on the suppressor side.
- **Offline judge pass (secondary, completed 2026-07-11):** all 75 deferred rubric queries scored with gpt-4.1 exactly as the harness prescribes (`src/eia_validation/judge_deferred.py` → `results/eia_judge_scores.json`). The official 0–2 rubric is insensitive to the edits: food_delivery all 0s, the_protector all 1s, the_listener all 2s (every condition, all 13 seeds), duel/maze varying by seed not condition. This agrees with the rule-based composite (null at rubric granularity) and validates the rule-based scorer against the official rubric; the discriminating signal lives below the rubric's resolution (say counts, door tradeoff).
- **Degeneracy check (2026-07-12, after Sol's critique):** listener messages across all 39 runs: unique-message ratio ≈ 1.0 in every condition (baseline 0.997 / positive_writers 1.000 / suppressors 0.997), max consecutive verbatim repeats = 1.0, mean message length flat (229 / 233 / 238 chars). Suppressor messages stay contextual late in runs (reference the user's specific disclosures). Rules out verbatim perseveration and a generic verbosity shift; does NOT rule out semantic repetitiveness (similar sentiments rephrased) or miscalibrated over-support — "more supportive verbal action," not "better help."
- **Interpretation (claim language of record, tightened per Sol 2026-07-12):** games evidence is **one-sided**: in Gemma-2-9b-it, removing the four suppressor pathways causes a large shift away from task persistence toward supportive verbal action in the EIA scenario with behavioral headroom; removing the positive writers produces no detectable in-game change at n=13, though it moves the forced-choice logit metric. This is **cross-format transfer** (textual decision pairs → interactive game actions), not fully unrelated OOD transfer — cell M scenarios share concepts with EIA. Floor/ceiling scenarios are **null/saturated outcomes**, not proven measurement artifacts. The asymmetry's explanation is open: candidate hypotheses (to be discriminated by E17/E18 + the distress-absent cells) include suppression-is-load-bearing, dose asymmetry (k=4 vs k=2 edited components), baseline gameplay sitting below a sampling threshold for positive-writer removal, positive pathways moving internal preference without leverage over free generation, and social-action persistence rather than calibrated helping. Bidirectionality remains established only at the forced-choice level (E13/E14b). Do not claim bidirectional *game* effects, "improved helping," or a generally prosocial policy.

### E17 · cross-model replication on Llama-3.1-8B-Instruct (gate 4) [C] — PRE-REGISTERED 2026-07-12, before any Llama runs
- **Model:** `meta-llama/Llama-3.1-8B-Instruct` (32 blocks, GQA 32 query heads / 8 KV, head_dim 128, **no sandwich norms** — component outputs write directly to the residual, so DFA needs no frozen-RMS factor and the effective edit direction is the direction itself). Code: model-family branch keyed on `hasattr(layer, "post_feedforward_layernorm")` in `direct_feature_attribution.py` and `weight_orthogonalization.py`; DFA's built-in exactness checks (head-sum vs block add, residual reconstruction) must pass on Llama before any downstream stage is interpreted.
- **Stage 1 (direction):** same V2.1 suite (audited A/B/D/E/F/G/H + templated M/T, tail pooling for M/T), `controlled_direction_analysis.py --blocks 8 12 14 15 16 18 20 24`. **Direction of record: cell-M direction at block B\* = argmax held-out cell-M AUROC among swept blocks; ties broken toward relative depth 20/42 ≈ 0.48 (block 15).** Gates: M held-out AUROC ≥ 0.85; transfer profile qualitatively matches Gemma (M→A and M→F clearly above chance, M→{E,T,G,H} near chance). If the transfer profile inverts (e.g., M→T high), stop and report — that's a replication failure at the representation level.
- **Stage 2 (DFA):** external direction at B\*, decision-tail pooling over M pairs. Writers = most positive `mean_paired_diff`, suppressors = most negative. Match Gemma k for comparability: k=2 positive writers, k=4 suppressors (deviation logged if the Llama profile is much more diffuse — decided by concentration metrics, before edit evals).
- **Stage 3 (edits):** rank-1 orthogonalization of the selected components against the B\* direction; primary outcome = decision_separation on held-out M pairs, prediction: positive-writer removal decreases, suppressor removal increases (bidirectional, same signs as Gemma E13/E14b).
- **Stage 4 (controls):** norm-matched random-direction controls on the same components (z-scores), capability spot-check (MMLU sample + wikitext ppl) under each edit.
- **Interpretation rubric (fixed now):** full replication = bidirectional stage-3 shift with stage-4 specificity; partial = one-sided shift (mirroring the E16b asymmetry would itself be informative); failure = no shift or checks fail → V2 claims narrow to a Gemma-2 case study. **Replication is functional, not anatomical** (per Sol, agreed): same functional organization (a mid-depth costly-helping direction with M-specific transfer, a sparse writer/suppressor decomposition, edits that move the decision metric) counts as replication even if layer/head identities differ — they are expected to.
- **If stage 5 (EIA games on Llama) runs:** scenario headroom is defined from **baseline runs only, before unblinding edit effects** (a scenario counts as informative if baseline outcomes are off floor/ceiling with nonzero variance); support quality/repetition metrics (unique-message ratio, message length, contextual reference) reported alongside counts, not counts alone; same parse-rate gates as E16.

#### E17 pre-registration amendments v2 (2026-07-12, after adversarial review by codex/gpt-5.6-sol; stage 1 running, nothing unblinded)
1. **Scenario-family leakage / circularity (accepted).** M = 5 scenario families × 8 templated variants; pair-random splits leak families across train/test, and the Gemma edit evals reused the same 40 M pairs that derived the direction (norm-matched controls protect direction-specificity, NOT in-sample-ness — this caveat now also attaches to the Gemma E13/E14b numbers). Remedies: (a) B\* selection and all direction AUROCs recomputed with **scenario-grouped splits** (whole families in train or test, never both) from the cached stage-1 activations before anything downstream is interpreted; the running stage-1's pair-random summary is treated as provisional. (b) **A fresh confirmatory M/T set will be generated (new scenario families, ≥5 M families / ~40 pairs, same template + matched-lexicon constraints, E10 audit) before stage 3**; the primary causal outcome is evaluated on this untouched set. The same confirmatory set will be run on Gemma retroactively (E14c) to patch the circularity caveat there.
2. **Winner's-curse in block selection (accepted).** B\* chosen by grouped-split AUROC; the ≥0.85 representation gate is then certified on the confirmatory set, not the selection split. Tie rule made numeric: blocks within 0.01 AUROC of the max are ties, resolved toward relative depth 0.48 (block 15). Blocks 8 and 24 count as mid-depth for selection purposes; selection outside 12–20 will be flagged as a depth mismatch with Gemma (20/42) in interpretation.
3. **Primary outcome corrected (accepted).** Primary causal outcome = **forced-choice helping logit delta (choice_scores) on the confirmatory M set**, with confirmatory T as negative control (prediction: |ΔT| ≪ |ΔM|) and A/F transfer as secondary positive controls. `decision_separation` (projection onto d at B\*) is demoted to a manipulation check — it is algebraically favored to move and cannot carry the replication claim. Code: `--block` now threaded through `weight_orthogonalization.py` and `norm_matched_controls.py` (was hard-coded 20).
4. **Numeric gates (accepted).** Representation gates at B\* (grouped splits): M AUROC ≥ 0.85; transfer M→A and M→F ≥ 0.65; specificity M→{E,T,G,H} ≤ 0.60. Component ranking statistic = `mean_paired_diff` (heads and MLPs compete in ONE ranking, as in Gemma). k deviation rule: if the top-k writer (resp. suppressor) set fails to reach 50% of the total positive (resp. negative) attribution mass at k=6, log a diffuseness deviation and still run k=2/k=4 as primary. DFA exactness tolerances: head-sum abs err < 1e-2 and residual reconstruction rel err < 1e-2 (Gemma/SmolLM observed ~1e-4).
5. **Dose asymmetry (accepted).** Both analyses pre-registered: (a) Gemma-matched k=2 writers / k=4 suppressors (procedural replication, primary); (b) dose-matched k=1..4 curves on both signs + comparison at matched realized Frobenius delta norm; plus individual-component effects (`run_individuals` exists). The bidirectionality verdict cites (b), not (a).
6. **Technical traps (accepted).** Raw-text prompts remain primary (matches Gemma; cross-model comparability); chat-template variant run once as sensitivity. A/B token single-token check already enforced (raises otherwise); option-order counterbalancing already in `choice_scores` (random flips). Decision-tail fallback: a pair whose tail mask is empty is EXCLUDED and counted, not silently pooled over the whole sequence; tail token counts reported. bf16 edits: record realized post-cast delta norm and post-edit residual alignment ‖dᵀW‖ (idempotence check: re-applying the edit must change ~nothing). Inference on paired deltas is **clustered by scenario family** (bootstrap over families, not pairs).
7. **Rubric tightened (accepted).** Full replication = bidirectional confirmatory-set forced-choice effects with norm-matched-random specificity on the same components AND random-component control AND T-selectivity. One-sided effect = partial replication regardless of resemblance to E16b; resemblance discovered afterward is not a success criterion.

#### E17 stage 1–2 results + amendment v3 (2026-07-12, codex-sparred; stage 3 not yet run)
- **Stage 1–2 (Spark, 02:12–02:38 EDT):** all cells extracted; grouped selection → **B\* = 15** (grouped CV saturated at 1.000 on every candidate block, anchor-driven — same saturation as Gemma; chain's provisional pair-random pick coincided). DFA at 15: exactness checks pass (head-sum 5.5e-5, recon 4.1e-4). Component ranking (one list, `mean_paired_diff`): writers k=2 = **L15MLP (+0.224, d=4.2), L12H4 (+0.120)**; suppressors k=4 = **L15H6 (−0.103, d=−4.6), L14H27, L12H20, L11MLP**. Functional shape parallels Gemma (dominant readout-block MLP writer + suppressor heads). Data: `results/controlled_directions_llama31_8b_it/`, `results/dfa_llama31_8b_it/`.
- **Diffuseness rule: threshold failed as written** (Llama k=6 mass 0.293 writers / 0.456 suppressors < 50%) — but the Gemma reference fails it too (0.155/0.372): the pre-registered threshold was miscalibrated (set without computing the reference value). Llama is *more* concentrated than Gemma. Logged as a pre-registration calibration error; k=2/k=4 primaries proceed as written.
- **Transfer gates at B\*=15: G and H FAIL.** A 0.817 ✓, F 0.982 ✓, E 0.288 ✓, T 0.142 ✓ (task-quietness — the core confound control — replicates cleanly), but G 0.821 ✗ (≤0.60), H 0.648 ✗, ungated D 0.91 (Gemma: G 0.53, H 0.17, D 0.24). **Stage 1 is recorded as a representation-specificity failure**: the Llama M direction functionally reads out caring-character/warmth contrasts that Gemma's did not. Cosines to those cells' axes are near zero (G 0.111, H 0.028, D 0.093), so subspace purification is unpromising — but low cosine does not prove unrelated mediation (covariance can carry AUROC); the specificity question moves to outcome level. (Correction of an initial overread on our side, per codex.)
- **Amendment v3 (codex verdict PROCEED-MODIFIED, adopted before stage 3):** stage 3 runs as a **mechanism replication attempt following a representation-specificity failure** — not a clean confirmatory replication, and a passing stage 3 cannot retroactively convert the failed G/H gates into "full replication." Raw M at 15 is the direction of record; purified variants are sensitivity only. Outcome-level specificity controls added: choice_scores on **D, G, H cells** (never used in derivation/selection; deviation from codex's fresh-families ideal, accepted for cost — logged) alongside confirmatory M (primary) and confirmatory T (negative control). **Selectivity margins fixed now:** "selective" requires |ΔM_confirm| ≥ 3× each of |ΔT_confirm|, |ΔD|, |ΔG|, |ΔH| with family-clustered bootstrap CIs; 2–3× = partial; any control moving with CI excluding 0 and ratio < 2 = non-selective. Persona-footprint rubric (codex): bidirectional M + quiet controls = costly-helping mechanism; D/H co-moving = warmth/persona; T co-moving = generic compliance; noisy controls = ambiguous, no specificity claim. Certification order: the ≥0.85 representation gate is scored on confirmatory M against the frozen direction BEFORE edit results are computed; baseline forced-choice headroom on confirmatory M recorded first. Random components (Gemma recipe, deterministic head+11 mod 32, verified outside DFA top-20): L1MLP, L12H15, L15H17, L14H6, L12H31, L2MLP.

#### E17 stage 3 results (2026-07-12 04:11 EDT) — MECHANISM DOES NOT REPLICATE ON LLAMA AT CONFIRMATORY LEVEL
- **Data:** `results/e17_stage3_llama31_8b_it/stage3.json` (script `src/e17_stage3.py`, --max-tokens 1024 after two caught bugs: missing shared_prefix on free-form cells; 512-token truncation that would have silently amputated D/G/H option B).
- **Gate A PASS:** frozen direction scores AUROC **1.000** on the untouched confirmatory M families (≥0.85 gate certified out-of-family). Representation transfers; behavior control does not (below).
- **Primary outcome (confirmatory M, family-clustered CIs): no condition moves it with CI excluding zero.** positive_writers_k2 −0.0169 [−0.034, +0.000]; suppressors_k4 −0.0013 [−0.033, +0.030]; targeted_k6 −0.0352 [−0.073, +0.004]; random_k6 −0.0013. Baseline M_confirm separation +2.93 → relative effects ≤1.2% (Gemma E14b was ~4.5% relative). All selectivity margins fail (T_confirm ratios 1.35–1.61).
- **The in-sample inflation signature (key finding):** on dev-M (the derivation pairs), positive_writers −0.0563* and targeted −0.0813* DO exclude zero — the writer-removal effect is 3–4× larger in-sample than on held-out families. This is exactly the inflation codex's circularity critique predicted, observed directly. **It puts the Gemma E13/E14b in-sample numbers under suspicion → E14c (Gemma edits on confirmatory sets) launched immediately, running now.**
- **Suppressor side: null, indistinguishable from random-component edits** (suppressor k-curve +0.016/+0.027/+0.051/−0.001 vs random k-curve up to +0.031). The Gemma push–pull structure does not appear in Llama forced choice. Individual edits show sign inconsistencies (L11MLP classed suppressor by DFA, removal decreases helping −0.059).
- **Checks all clean:** idempotence ≤0.014, neutral KL 6e-4, top1 agreement 1.0 — edits are small, well-formed, and behaviorally inert on neutral prompts; the null is not an implementation artifact by these measures.
- **Verdict per pre-registered rubric (amendment v3): replication failure at the causal-mechanism level** (stage 1 was already a representation-specificity failure; stage 3 adds no confirmatory-level behavioral control in either direction). Pending E14c, the V2 weight-edit claims narrow toward a Gemma-2 case study whose strongest leg is the E16b in-game behavioral result (which is out-of-family by construction — games, not text pairs).

#### E14c interpretation rubric + E17b null-audit plan — PRE-REGISTERED 2026-07-12 ~05:30 EDT, before E14c results seen
- **E14c rubric:** (a) Gemma confirmatory-M deltas keep sign with CIs excluding 0 and ≥2× selectivity → mechanism survives out-of-family; story = "real in Gemma, not conserved in Llama." (b) Effects shrink >2× with CIs including 0 → in-sample inflation generalizes; forced-choice weight-edit claims retract to in-sample-only; E16b games become the primary causal evidence and the games-vs-choice dissociation becomes the object of study. (c) Intermediate → report both, no strong claim.
- **E17b (codex null-audit, verdict NULL-POSSIBLY-ARTIFACT; key JSON facts: manipulation check moved −12.7%/+11.9% bidirectionally while behavior stayed flat; saturation refuted by margin-vs-delta correlations −0.085/−0.255 going the wrong way for a ceiling account):** two decisive tests, in order: (1) **chat-template sensitivity** (pre-registered in amendment 6) — same conditions on confirmatory M/T under `apply_chat_template`; bidirectional random-specific effects would rescue the mechanism, format artifact confirmed. (2) **direction ablation at the activation level** — project out the frozen direction from block-15 residuals (and separately at late blocks) during choice scoring; if activation-level removal moves choice but weight edits don't, rank-1 component edits are insufficient/bypassed; if neither moves choice, gate A is decodability-without-causal-use and the null upgrades to NULL-LIKELY-REAL. Interpretation fixed before running.

#### E14c results (2026-07-12 04:53 EDT) — GEMMA SURVIVES OUT-OF-FAMILY; SUPPRESSOR AXIS REINTERPRETED
- **Data:** `results/e14c_gemma2_9b_it/stage3.json` (same runner/protocol as E17 stage 3; Gemma components, direction of record at block 20). Gate A: confirmatory-M AUROC 1.000. Baselines: M_confirm +4.47, T_confirm +3.20.
- **Writer mechanism: fully survives, helping-selective.** positive_writers_k2 on confirmatory M: **−0.1836, CI [−0.236, −0.133], all selectivity ratios ≥ 4.3** (T 6.9, D 4.3, G 19.8, H 6.2) — passes the pre-registered 3× bar out-of-family. Dev −0.214 vs confirm −0.184: ~14% shrinkage only. **No in-sample inflation in Gemma** (unlike Llama) — the E13/E14b circularity caveat is discharged for the writer side.
- **Suppressor mechanism: survives on M but is NOT helping-selective.** suppressors_k4: M_confirm **+0.1497*** (right sign, out-of-family) BUT **T_confirm −0.1766*** — suppressor removal also shifts the task-persistence cell toward the distraction, at equal magnitude. Reinterpretation (new hypothesis, to be tested): the push–pull structure decomposes as **writers = helping-specific; suppressors = objective-adherence/task-persistence** (not helping-specific). This rereads E16b: the listener-game behavior (abandon door, keep engaging) is compatible with reduced objective-adherence rather than increased helping per se — though the maze seed-42 counterexample (only suppressor-edited model completed the task) argues against a pure persistence-loss account. E18's cost axis + non-social-alternative cells discriminate.
- **Targeted k6 cancels on M** (+0.004; −0.18 writers + 0.15 suppressors) while leaving T shifted (−0.142*) — consistent with two distinct axes summing.
- **Complication (report honestly):** the random-COMPONENT control (target direction removed from 6 arbitrary components) moves M_confirm −0.0573* — direction removal leaks weakly from anywhere; the writer effect is 3.2× larger. The E14b norm-matched *random-direction* control (same components) remains the direction-specificity evidence; this random-component result bounds component-specificity at ~3×, not ∞.
- **Manipulation-check double dissociation across models:** Gemma suppressors move behavior (+0.15) with ~0 projection change (+0.7%); Llama suppressors move projection (+11.9%) with ~0 behavior. The block-B* M-projection is NOT the behavioral mediator for suppressor effects — consistent with the suppressors acting on a different (persistence) axis whose readout is elsewhere.
- **Cross-model verdict per rubric:** Gemma = real, out-of-family, writer-side selective (rubric a for writers; intermediate c for suppressors). Llama = no causal control at any confirmatory level (E17 stage 3), same protocol/sets — a genuine architecture/training difference, pending E17b's format/ablation checks on the Llama side.

#### E17b results (2026-07-12 06:30 EDT) — LLAMA DIRECTION IS CAUSALLY USED; COMPONENT EDITS UNDERDOSE IT
- **Data:** `results/e17b_null_audit_llama31_8b_it/e17b.json` (3-minute run; the earlier "2-hour E17b" was a zombie launch that died in 1s on a silently-failed git pull — see safe_pull.sh note).
- **Test 2 (decisive): activation-level ablation of the frozen direction at block 15, unedited weights → dM_confirm −0.1602, CI [−0.188, −0.130], with T quiet (+0.013).** The costly-helping direction IS causally read in Llama, selectively, at ~5.5% of baseline separation — same relative scale as Gemma's writer weight-edits. Late-block ablations (20/24/28) are near-zero → causal consumption happens at/just after B15.
- **Test 1 (chat template):** writer-removal strengthens under chat format: −0.0768* (vs −0.029* raw; baseline M rises 2.93→5.12). Suppressors null in BOTH formats (−0.013 chat / +0.012 raw, CIs include 0). Random null.
- **Synthesis per the pre-registered decision tree:** the E17 stage-3 null decomposes into (a) **underdosing** — rank-1 edits on k=2 writer components remove only ~20–50% of what full-direction ablation removes; consistent with distributed writing (k=6 = 29% of positive attribution mass); (b) **suppressor-side genuinely null** — Llama has no causally effective suppressor components among the DFA-negative set; (c) **format contribution** — chat roughly doubles the writer effect. Notable tension for methodology: Llama's DFA attribution mass looked MORE concentrated than Gemma's, yet its component edits capture less behavior — attribution mass ≠ causal contribution.
- ~~Revised cross-model statement (implementation geometry / component concentration)~~ **RETRACTED same day** — the Gemma ablation normalization below shows the difference is direction-level dependence, not component concentration; Gemma's edits also capture only ~5% of its full-direction effect.

#### E18b: cross-model fractional ablation curves (2026-07-12 06:50 EDT) + corrected synthesis (codex SYNTHESIS-NEEDS-FIX adopted)
- **Data:** `results/e17b_frac_llama/e17b.json`, `results/e17b_frac_gemma/e17b.json`, `results/e17b_ablation_gemma2_9b_it/e17b.json`. Fractions 0.25/0.5/0.75/1.0 of the direction projection removed at B\*, raw format, confirmatory M/T, plus 3 norm-1 random-direction full ablations per model.
- **Both models: linear, monotone dose curves; random directions null.** Llama B15: −0.035/−0.077/−0.116/−0.160 (T quiet ≤+0.016; random-dir −0.020/+0.001/−0.016 ns). Gemma B20: −0.88/−1.99/−3.09/−3.94 (random-dir ±0.03 ns; dT grows to +0.58 at f=1 — full ablation not fully selective in Gemma).
- **Corrected cross-model statement (assay-bounded, per codex):** on this forced-choice assay, **Gemma's decision score is heavily dependent on its identified B20 direction (full removal erases ~88% of baseline separation), while Llama's analogous B15 direction carries only ~5.5%** — implying substantial causal support outside that one-dimensional subspace in Llama. The linear dose-response with null random-direction controls in both models rules out a full-ablation geometric artifact. NOT claimed: "mediation" in the strict sense, or "Llama computes it elsewhere" as a mechanism.
- **Percentage discipline (per codex, adopted):** Gemma writer-edit = 4.7% of its full-direction effect (legitimate scale comparison); Gemma suppressor effects are NOT expressible as a fraction of the M-direction ablation (different axis — their M-projection barely moves, E14c); Llama raw writer-edit ≈ 18–20% of raw ablation (the earlier ~50% figure mixed chat edits with raw ablation — retracted).
- **Kept straight (per codex):** the two Gemma edit families have complementary, non-interchangeable evidence — writers are choice-selective (E14c) but were null in games (E16b positive_writers); suppressors transfer to games (E16b) but are choice-non-selective (E14c T co-movement, now explained by E18's flat capture component). No claim may blend these.
- **State of the synthesis:** writers = cost-independent, welfare-preferential value writing, validated out-of-family, choice-assay-selective. Suppressors = cost-gated inhibition of welfare action + flat person-engagement capture, validated out-of-family with in-game behavioral transfer. Direction-dependence of the choice computation differs ~16× between Gemma and Llama (assay-bounded). All claims carry their pre-registered controls; deviations and retractions logged inline above.

#### E18 results (2026-07-12 06:34 EDT) — THE PUSH–PULL DECOMPOSES: COST-INDEPENDENT PROSOCIAL VALUE (WRITERS) vs COST-GATED SUPPRESSION (SUPPRESSORS)
- **Data:** `results/e18_interaction_gemma2_9b_it/e18.json` (`src/e18_interaction.py`; Gemma, both V2.2 axes, family-clustered, joint-family interaction bootstrap).
- **Stimulus validation:** baseline uptake falls monotonically with cost in both cells (welfare 4.61→−1.71; nonsocial 2.86→−4.06) and welfare adds value at every level — the cost axis works as designed; the model integrates both cost and welfare at baseline.
- **Suppressors (the money result): the effect is COST-GATED and welfare-preferential.** On the welfare axis the k=4 suppressor-removal effect grows with cost: +0.06 (free) → +0.13 → +0.53 → +0.49 (dose slope +0.17); on the matched non-social axis it is flat (~+0.2, slope −0.002). Interaction (welfare − nonsocial) = **+0.097, CI [+0.026, +0.169]**. Reading: the suppressor components implement **cost-dependent inhibition of welfare-motivated action** (barely active when helping is free; the thing holding the model back when helping is costly) **plus** a smaller flat person-engagement capture component (which is what co-moved T_confirm in E14c). This lands the original falsification-table row "effect only when helping is costly → task-vs-welfare arbitration" — with the E14c/E16b caveats now explained rather than anomalous.
- **Writers: cost-INDEPENDENT prosocial value.** Welfare-axis effect ≈ −0.20 at every cost level (slope −0.013), larger for welfare than nonsocial (interaction **−0.093, CI [−0.117, −0.070]**). Writers carry "helping people is valuable," independent of price; suppressors carry "the price gate."
- **Functional semantics of the push–pull (claim language of record):** removing writers makes helping less attractive everywhere (welfare-preferentially); removing suppressors disinhibits *costly* helping specifically, with dose-response in cost. The E16b listener result (a high-cost scenario) is the in-game enactment of the suppressor dose-response. targeted_k6 = superposition of both (net disinhibition at high cost: −0.09 free → +0.32 high).
- **Controls:** random_k6 near-zero overall, interaction +0.018 [0.004, 0.035] — the known direction-leak, 5× smaller than either targeted interaction; report with scale.

#### Gemma retro-check under amended selection (2026-07-12)
- `src/analysis/grouped_block_selection.py` (codex-reviewed: FIX verdict addressed — NPZ row provenance, alignment fingerprint, equidistant tie rule, validation) run on the cached Gemma activations: grouped leave-one-family-out CV AUROC = **1.000 at every candidate block** (8–31), so selection is anchor-driven → **block 20, identical to the block of record**. Transfer gates at 20 (grouped direction): A 0.709 ✓, F 0.904 ✓, E 0.539 ✓, T 0.023 ✓, G 0.530 ✓, H 0.169 ✓. The Gemma block choice survives the leakage-hardened procedure; the remaining Gemma caveat is edit-eval circularity, to be patched by E14c on the confirmatory M/T sets. Data: `results/controlled_directions_gemma2_9b_it/grouped_selection.json` (Spark).
- E18 (planned): factorial decomposition of the edited mechanism — Need × Cost × Alternative-claim grid (`notes/v2_2-factorial-design.md`, issue #34); edited models {baseline, positive_writers, suppressors} across graded cost + moral-vs-moral cells; headline figure = helping-rate vs cost dose-response per edit condition.

### E19 · cell R robustness analysis (2026-07-12) [C] — STIMULUS ARTIFACT FOUND; ROBUSTNESS UNDETERMINED
- **Run:** `src/analysis/cell_r_robustness.py` on Gemma B20 frozen direction → `results/cell_r_robustness_gemma2_9b_it/cell_r.json`. R overall AUROC **0.467 [0.40, 0.53]** (paraphrase 0.415, name_swap 0.433, register_shift 0.568) vs unperturbed cell-A reference 0.726.
- **Not a robustness failure — a generation artifact.** Direct inspection: the R *neg* texts conclude with helping (e.g., "The actual accomplishment lies in demonstrating fundamental human compassion… That's the result I'm directing my efforts toward"), i.e., both sides express the same decision. The haiku perturbation pass rewrote away the decision contrast. Cell R was never E10-audited — the exact failure mode the audit exists for. Chance AUROC is the *expected* output for no-contrast pairs.
- **Consequence:** the Lazar robustness criterion remains **unassessed**. Fix: regenerate R with contrast-preserving instructions (perturb surface form only; each side's decision conclusion preserved in meaning), audit for retained contrast before use, then rerun this analysis.

### E19b · cell R v2 robustness — DIAGNOSTIC PASS (2026-07-12) [C]
- **Regeneration:** `src/data_generation/regenerate_cell_r.py` (codex FIX review adopted: 4-way decision judge + preservation judge (sonnet), deterministic name_swap, pair-level matchedness gates, neutral retry, all-source calibration). **Calibration finding:** only 13/34 audited A pairs pass strict immediate-action calibration (12 both-sides-help, 7 both-sides-task, 2 ambiguous) — the E10 audit checked label polarity, not decision classification; cell-A neg sides often deliberate toward helping. Output: 29 audited pairs (10 name_swap / 9 paraphrase / 10 register_shift), `data/contrastive_pairs/v2_1/R_v2_contrast_preserving.jsonl` (+ attrition report).
- **Result:** frozen B20 direction on perturbed pairs: **overall AUROC 0.911 [0.82, 0.98]** — name_swap 0.900, paraphrase 0.901, register_shift 1.000 — vs matched unperturbed reference on the same calibrated sources **0.827**. Separation fully survives surface-form perturbation. Data: `results/cell_r_v2_robustness_gemma2_9b_it/cell_r.json`.
- **Claim (bounded as pre-agreed):** DIAGNOSTIC robustness pass — n=13 source pairs, one perturbation set, one model/block; adequate to close the E19 artifact and support "not lexically brittle," not an equivalence claim. Lazar robustness criterion: satisfied at diagnostic grade. (Reference-matching caveat: source-key join matched 15 A rows for 13 keys — pair_index not unique per scenario; numbers are per-row.)

### E20 · GemmaScope decomposition of the block-20 direction (Stage B, first SAE result) [C] — 2026-07-12
- **Script/data:** `src/analysis/sae_direction_decomposition.py` → `results/e20_sae_gemma2_9b_it/e20_sae.json`. GemmaScope IT residual SAE, layer 20, width 16k, canonical. Two views: decoder-cosine with the frozen direction; per-token feature-activation paired diffs on M decision tails. **16 features overlap between the two top-30 lists** — geometry and behavior converge.
- **Post codex retro-review (FIX, adopted):** the activation view was token-weighted (longer tails weigh more) — pair-level re-aggregation pending; the two views share the M decision tails so the 16-feature overlap is partly expected by construction (no overlap null run yet); "independently recover the E18 semantics" is retracted to **"selected auto-labels are qualitatively compatible with the welfare-vs-task-persistence reading"**; all 16 overlap labels are in the JSON (7 legible ones narrated below; f1295 kept in the denominator). Exploratory.
- **Named features (Neuronpedia auto-interp), help side (+):** f13551 "pausing and resuming processes/activities" (+3.69) — matches the help clauses' pause-the-task move; f6128 "personal sacrifice and heroism" (+2.57); f4567 "relational/emotional family dynamics" (+2.20); f2585 "community engagement" (+1.97); f7773 "certainty/commitment to fulfill" (+3.52). **Task side (−):** f8395 "ambition and perseverance" (−3.20); f1876 "sports and team outcomes / winning" (−2.46). The named poles — *sacrifice/pause/care* vs *ambition/perseverance/winning* — are qualitatively compatible with the E18 reading (welfare value vs task-persistence gating); labels persisted in `neuronpedia_labels.json`.
- **Caveats:** all decoder cosines are small (|cos| ≤ 0.19) — the direction is distributed over many features, consistent with E09/E12 diffuseness; the largest-diff feature (f1295, auto-labeled "programming/web development") is likely a mislabeled/polysemantic feature — flagged, not interpreted; auto-interp labels are unreliable in general; this is **correlational feature naming**, not feature-level causal evidence (feature ablation/steering would be the upgrade).

### E21 · need axis (Lazar sensitivity) — PRE-REGISTERED 2026-07-12 before any model runs on the new cells
- **Cells:** need gradient = resolved (past setback, explicitly no current need) → mild (minor setback, discouragement) → urgent (= existing cost axis); excited cell retained as salience control only, NOT a need level (its judged need 2.50 sits above resolved). All arms share the 10 frames, 4 cost stems, and universal decision clauses; the need manipulation lives only in the message description + acknowledgment. Mild ack's remedy-scale phrase removed pre-run (codex: proportionality confound with the fixed several-minutes decision clause). **Manipulation check passed before any model run:** judged perceived need (sonnet, n=20/arm, cost-free texts) = resolved 2.00 < excited 2.50 < mild 3.40 < urgent 4.50 (`data/contrastive_pairs/v2_2/need_pretest.json`).
- **Primary contrasts (3, fixed now):** (1) baseline monotone need trend resolved<mild<urgent, cost treated categorically (ordered trend secondary); (2) suppressor-k4 edit × need interaction (prediction: effect grows with need); (3) writer-k2 edit × need interaction (prediction: reduction grows with need; flat in cost). Full edit×need×cost term reported descriptively. Inference: family-level units (variants averaged within family-cell), family bootstrap + leave-one-family-out sensitivity; logit scale throughout.
- **Lazar mapping:** baseline contrast (1) = sensitivity criterion; edit contrasts (2)-(3) test whether the mechanism carries the sensitivity.

### E21 results (2026-07-12 10:21 EDT) — NEED AXIS: SENSITIVITY ESTABLISHED AT BASELINE AND IN BOTH MECHANISMS
- **Data:** `results/e21_need_gemma2_9b_it/e18.json` (4 cells × 5 conditions, per-pair deltas persisted for LOFO).
- **Contrast 1 (baseline need trend): PASS, large.** Uptake grades monotonically with judged need: resolved −4.53 < mild −0.01 < urgent +1.74 (excited control −0.85, matching its judged-need rank between resolved and mild). Lazar **sensitivity** criterion satisfied at baseline — 6.3 logits of dynamic range.
- **Contrast 2 (suppressor × need): PASS.** Suppressor-removal effect grows monotonically with need: +0.091* (resolved) → +0.260* (mild) → +0.303* (urgent); trend +0.212, LOFO range [+0.185, +0.231], sign-stable. The nonzero resolved-arm effect (+0.091) is the flat engagement component again (cf. E14c/E18).
- **Contrast 3 (writer × need): PASS, cleaner than predicted.** Writer-removal effect: **+0.019* (resolved, ≈ zero)** → −0.195* (mild) → −0.222* (urgent); trend −0.241, LOFO [−0.249, −0.231], sign-stable. At zero current need the writers contribute nothing to person-uptake — the "prosocial value" they write is **need-gated at the value level**, not unconditional person-attraction.
- **Random control:** flat across need (−0.007/−0.013/−0.006, all CIs include 0), trend +0.002.
- **Refined mechanism semantics (claim language of record):** writers write **need-sensitive welfare value** (cost-independent, ≈0 at zero need); suppressors gate action with sensitivity to both need and cost, plus a small unconditional engagement term. Both mechanisms carry the sensitivity criterion, not just the baseline representation.

### E22 · moral-vs-moral axis — PRE-REGISTERED 2026-07-12 before any model runs (codex design review: FIX round adopted)
- **Stimuli:** `data/contrastive_pairs/v2_2/moral_axis_templated.jsonl` (10 families × 3 relative-need levels × 4 variants = 120 pairs). Agent helping P1 (constant, with a uniform lossless-pause sentence isolating allocation order from harm-to-P1); P2's single contact varies only in stakes (contact framing identical across arms; no urgency adverbs; no wait-permission; lower arm = genuine milder current need, not convenience). pos = switch to P2 now / neg = finish with P1 first; both options commit to helping both.
- **Manipulation pretest (before model runs):** P2-need 2.20 < 3.00 < 4.20 ✓ monotone. **Known limitation (logged, not fixable within this axis): respond-now pressure co-varies (2.00/2.60/4.30)** — the axis measures composite current claim strength (need + immediacy), as real moral claims do.
- **Analysis units:** variant-averaged family×level cells; joint family bootstrap across levels/conditions; LOFO on every headline contrast; contrasts low→equal, equal→high, high−low, curvature.
- **Pre-registered contrasts + interpretation table:**
  1. Baseline relative-need slope (high−low) > 0 expected (triage tracks claim strength). Equal-arm value is DIAGNOSTIC, not a prediction: negative = incumbency/continuity prior; ≈0 = need-equality offsets it; positive = recency/salience bias.
  2. Suppressor-k4: mean switching boost tested separately from slope change. **Incumbent-stabilization reading = boost with |Δslope| < 0.05/level (equivalence bound, fixed now); welfare-triage reading = Δslope > 0 with CI excluding 0.** Both may hold (mixed).
  3. Writer-k2: prediction = baseline high−low slope reduction (need-gated value flattens P2-need responsiveness).
  4. Random-k6: all effects ≈0; any slope change bounds the leak.

#### E22 results (2026-07-12 10:45 EDT) — MORAL-VS-MORAL: INCUMBENCY PRIOR + FLAT SUPPRESSOR BOOST; GATE IS TASK-SPECIFIC
- **Data:** `results/e22_moral_gemma2_9b_it/e18.json` + `baseline_per_pair.json`.
- **Contrast 1 (baseline slope): PASS.** Switching preference tracks P2's claim strength: lower −2.61 < equal −2.46 < higher −1.27 (high−low = +1.34). **Equal-arm diagnostic: strongly negative (−2.46)** → a large incumbency/continuity prior — at equal need the model prefers finishing with the person it is already helping by ~2.5 logits. (Per the pre-registered table: incumbent/continuity prior, not indifference.)
- **Contrast 2 (suppressors): incumbent-stabilization signature.** Mean switching boost +0.038, CI [+0.021, +0.059], with slope +0.020 flat-compatible under the pre-registered |Δslope|<0.05/level bound (CI includes 0; LOFO [−0.002, +0.040]). Suppressor removal loosens the incumbency hold regardless of relative need — welfare-triage steepening NOT observed. **Scale finding: the boost is ~8× smaller than in task-vs-welfare cells (+0.038 vs +0.30)** — the suppressor gate defends task objectives far more than moral incumbencies.
- **Contrast 3 (writers): null.** Mean −0.067* flat across levels; Δslope −0.016 flat-compatible → no detectable flattening of P2-need responsiveness. With both parties carrying welfare value, writer removal nets a small uniform reduction in switching; the need-gated-value prediction is not confirmed in the moral-moral regime at this n.
- **Contrast 4 / caveat (weak-effect regime):** random-k6 moves −0.035* — comparable magnitude, opposite sign to the suppressor boost. At this scale the direction leak is non-negligible; inference rests on the sign pattern (suppressors +, writers −, random −) and the suppressor-vs-random difference (+0.073), not on magnitudes alone. Report as such.
- **Interpretation (claim language of record):** the V2.2 factorial is complete. The suppressor gate is principally a **task-vs-welfare** arbitration mechanism (E18 cost gating, E21 need scaling) with only a small, need-insensitive spillover onto moral-vs-moral allocation — consistent with incumbent-policy stabilization and inconsistent with a general welfare-triage weighting. Editing these weights does not detectably reweight triage between two moral claims.

### E23 · pre-prose stress test (codex, 2026-07-12) — FIX-FIRST verdict; fixes executed [C]
- **Codex full-chain audit before paper prose.** Numerical spot-checks of E14c/E16b/E17b/E18/E18b/E21/E22 all reproduce. Five substantive findings, all acted on:
  1. **E18 estimand mislabel (fixed, E18c):** the reported "interaction" was the axis-mean difference, not a slope interaction. Correct estimand (`src/analysis/e18c_slope_interaction.py` → `results/e18c_slope_interaction_gemma2_9b_it/`): suppressor cost-slope difference (welfare − nonsocial) **+0.172, CI [+0.129, +0.220], positive in 10/10 families**; writers −0.059 [−0.081, −0.039], 0/10 positive; random 0.000 [−0.019, +0.019]. The corrected statistic is stronger. **Need×cost surface now explicit:** suppressor cost-slope by need = −0.033 (resolved) / +0.055 (mild) / +0.170 (urgent) — the cost gate is **conjunctive on need**, reversing sign at zero need. "Cost-independent writers" holds within each need level (slopes +0.021/+0.042/−0.013) but is scoped accordingly.
  2. **E16b discovery pooling (disclosed, new-seeds split persisted):** on the 10 seeds untouched by discovery (`e16b_newseeds_confirmatory.json`): suppressor say-delta **+5.0 [1.1, 8.7], sign-test p=.039 — confirmed out-of-discovery**; the door contrast is 0/10 vs 0/10 (baseline floor, no headroom) — **the 0/13-vs-2/13 door headline is discovery-data only** and is demoted from headline status.
  3. **E14b z-scores must not be attached to E14c effects** (cross-run scale rule violated in the draft abstract) → E14d rerunning norm-matched random-direction controls under the E14c protocol (confirmatory sets, in-run recompute), running now.
  4. **Single random-component set is a comparator, not a null** (it moves outcomes significantly in E14c/E18/E22) → E14d also samples 12 random k=6 component sets for a proper null distribution.
  5. **Provenance:** missing artifacts swept from Spark (E01/E02/E04/E05/E14/E14b/E17-stage1-2 JSONs + direction .npys; activations too large for git, hashed in place); `results/PROVENANCE.json` (sha256, 322 files); E19b matched reference persisted (`matched_reference.json`; join caveat: 15 rows / 10 unique keys); E20 Neuronpedia labels persisted; E20 log self-contradiction fixed.
- **Also flagged, disclosed in paper:** Llama raw writer effect differs across nominally identical runs (−0.0169 E17 vs −0.0287 E17b; bf16 run-to-run nondeterminism suspected — small-effect numbers carry ~±0.01 run noise); "functional writers/suppressors" language calibrated to "component sets exhibiting writer-like and need-gated-gating-like intervention profiles on these assays."

### E14d · controls under the E14c protocol + the padding bug (2026-07-12) [C]
- **Bug found and fixed (root cause of the E14/E14b 4× scale mystery):** `norm_matched_controls.py` never set `padding_side="right"`; Gemma defaults left, so `choice_scores` read logits at a mid-sequence position. E14/E14b absolute scales were mis-indexed (their z-scores remain directionally meaningful since null and targeted shared the readout, but their magnitudes are deprecated). First E14d attempt reproduced the bug (deltas −0.024/+0.021, 8× too small) — diagnostic confirmed by fix: post-fix baseline 4.4661 matches E14c exactly.
- **E14d-a (norm-matched random directions, confirmatory M/T, in-run recompute):** targeted −0.1927 / +0.1471 (matching E14c within run noise); random-direction nulls +0.002 ± 0.019 / −0.003 ± 0.020 → **z = −10.5 (writers), z = +7.7 (suppressors)**. Direction-specificity now established under the same protocol as the headline effects. `results/e14d_gemma2_9b_it_normmatched_v2/`.
- **E14d-b (random-component null, 12 sampled k=6 sets):** −0.034 ± 0.024, range [−0.066, +0.009] — systematically slightly negative (removing the direction from anywhere bleeds a little helping value); the historical single random set (−0.057) sits inside this null. Writer −0.184 → **z = −6.2**; suppressor +0.150 → **z = +7.7** vs the sampled null. `results/e14d_gemma2_9b_it/random_component_sets.json`.

### E24 · AUROC polarity bug — MAJOR REINTERPRETATION (2026-07-12, external review [S]; rescue sequence executed)
- **The bug:** every specificity gate and every "quiet/near-chance" reading treated LOW AUROC as silence. AUROC is one-sided: 0.023 on cell T is **0.978 two-sided separability with inverted polarity**. Corrected two-sided profile of the Gemma B20 M-direction: **T 0.978-inverse, H 0.831-inverse, D 0.756-inverse, B 0.661-inverse**, E 0.539, G 0.530, A 0.709, F 0.904. The direction was never purified away from task persistence — it substantially IS a task-interruption axis (with welfare loading). Gate code corrected to `|auroc−0.5| ≤ 0.10` (`grouped_block_selection.py`); all "near chance on T/D/H" claims RETRACTED (E08's transfer-profile interpretation, paper §4.1, showcase site).
- **What survives untouched (per the same review):** all causal effect sizes (E14c reproduces across every confirmatory family), norm-matched same-component random-direction controls, the E18/E18c cost-gate and E21 need interactions (recomputed family-wise: urgent−resolved slope difference +0.203 [+0.146,+0.259], 10/10 families), capability bounds, provenance (324/324 hashes), family disjointness, and three family-clustered mediation edges.
- **What does not survive: the NAMES.** "Welfare-value writers + welfare cost gate" presumed a welfare-purified direction. The defensible statement pending revalidation: *rank-1 edits to a small late-layer component set alter task interruption vs persistence in response to distress, with one-sided transfer to conversational action allocation.* The welfare-specific evidence (E18 axis contrast, E21 need scaling, E22 boundary) still argues for welfare content in the mechanism — but component selection began from a mixed direction, so identities need re-derivation.
- **Additional corrections adopted:** "writer effect vanishes at resolved need" → "+0.019 [+0.003,+0.037], small, sign-opposed, nonzero"; the "free" cost arm confounds zero cost with no-active-objective (excluding it strengthens both effects: suppressors +0.235 [+0.171,+0.306], writers −0.078); random-component null was not matched by k/type/layer/norm (rescue step 5); game statistic is conversational-persistence allocation, not calibrated help (rescue step 6).

### E25–E27 · rescue sequence status (2026-07-12, after E24)
- **Step 1 ✓** two-sided gates + retractions (E24, commit c5d5c6a).
- **Step 2 ✓ (E25):** residualized direction d_resid = M ⊥ {T,E,D,H} at B20 **passes all two-sided gates** (A 0.777, F 0.966, M 1.00; T 0.552, D 0.526, H 0.494, B 0.569, E 0.593; exception logged: G 0.746 elevated — motive content, arguably legitimate welfare loading). The factorial DiD candidate (welfare−nonsocial with identical decision clauses) FAILS quiet gates (T 0.97) and is rejected by the fixed selection rule. `results/controlled_directions_gemma2_9b_it/e25_directions.json`, `direction_M_resid_block20.npy`.
- **Step 3 (running):** DFA with d_resid passes exactness checks; new pre-registered-rule component sets: writers **L20MLP, L19MLP**; suppressors **L18H13, L20H10, L19H12, L17H7** (L19MLP, L18H13, L19H12 retained from the old sets). Held-out edit protocol (e17_stage3, confirmatory M/T + D/G/H) in progress → `results/e25_stage3_resid_gemma/`.
- **Steps 4–6 (armed, chained behind step 3 on the Spark):** e26_format_stress (raw/chat/3 clause paraphrases/continuation-likelihood readout), e26_matched_nulls (writer-matched k=2 MLP+head and suppressor-matched k=4 heads, realized delta norms), e27_game_variants (listener with distress/excited/resolved message pools × baseline/suppressor, 8 seeds) + content-aware say classification (`score_e27.py`).

#### E25 stage-3 results (rescue step 3, 2026-07-12 17:10 EDT) — PURIFIED WRITERS ARE STRONGER AND CLEANER
- **Data:** `results/e25_stage3_resid_gemma/stage3.json` (d_resid, new component sets, confirmatory outcomes).
- **Writers (L20MLP+L19MLP): dM_confirm −0.284 [−0.352, −0.221], selectivity T 20.2× / D 4.4× / G 61.3× / H 4.5× — passes the 3× bar on every cell.** vs the contaminated derivation's −0.184 at T 6.9×. Dev −0.273 vs confirm −0.284: no inflation. The welfare-writer identity is re-established on a task-quiet direction, stronger than before.
- **Suppressors (L18H13,L20H10,L19H12,L17H7): +0.158 [+0.111, +0.208] but T ratio 1.07** — task persistence co-moves at parity even under the purified direction. The gate's task-coupling is a property of its FUNCTION (as E18's factorial showed), not an artifact of direction contamination.
- **Random comparator: −0.083* [−0.120, −0.042]** — larger than the historical set; writer = 3.4× random. Structure-matched nulls (step 5, running) are the proper judge.
- **Rescue verdict so far:** the E24 bug changed which components are the writers (L20MLP in, L20H15 out) and materially improved the headline: the welfare-value writer claim now rests on a direction that is genuinely quiet (two-sided) on task/warmth/character.

### E25b–E28 · second external review [S] — corrections + revalidation chain (2026-07-12 evening)
Sol's verdict on the rescue-in-progress: "Progressing, Not Yet Revalidated." Verified each claim against code; all four critical findings CONFIRMED and adopted:
1. **E25 gate coverage bug (confirmed, fixed):** `build_residualized_direction.py` gated quietness only on E,T,D,H while E24's corrected gate set is B,D,E,G,H,T (`gates_ok` now covers all six + reports `failed_quiet_cells`). Recomputed verdict over the existing profiles (`e25_directions.json` updated in place): **d_resid FAILS the G gate (0.746 two-sided; genuine-vs-instrumental caring motive); under the pre-registered selection rule `chosen = None` — no candidate fully passes.** The prior entry's "passes all two-sided gates (exception: G)" language is RETRACTED — a failed motive gate is not an exception. d_resid remains the working direction with G-separation reported as an open confound. Defensible claim (Sol's wording, adopted): *two late MLPs selected from a task/warmth/character-residualized direction causally and selectively reduce the forced-choice preference for interrupting a task to respond to distress* — NOT "welfare-value writers" until held-out motive/persona controls pass.
2. **In-sample quietness (confirmed):** d_resid was residualized against T/E/D/H and profiled on the same cached cells; quietness there is development evidence. New `certify_direction_heldout.py` certifies on confirmatory held-out families (M_confirm positive, T_confirm quiet) — chained as step 5b. Held-out D/H/G/B families do not exist; gap stays open until fresh generation. The docstring's promised-but-absent LOFO is removed in favor of this held-out certification.
3. **Stale headline reuse (accepted):** direction + writers changed completely, 2/4 suppressors changed → E18/E21 factorials, capability, and norm-matched-direction results from the old sets DO NOT validate E25. `capability_eval.py` + `e18_interaction.py` now take `--writers/--suppressors` overrides; fresh runs for the new sets chained as steps 7–8 (`results/capability_eval_resid_gemma/`, `results/e18_interaction_resid_gemma/`).
4. **Writer-matched null was not writer-matched (confirmed, run killed):** `e26_matched_nulls.py` hardcoded k_mlp=1,k_head=1 while the E25 writers are 2 MLPs. The step-5 run in flight was killed (~35 min burned, results discarded). Fix: null composition now DERIVED from the targeted set; layer band widened ±3 (exact range leaves only the 2 targeted MLPs — zero candidates); null sets distinct, and small spaces enumerated — the writer arm runs all C(6,2)=15 two-MLP sets in layers 16–23, making `n_more_extreme` an exact permutation count (min p = 1/16; z demoted to coarse secondary).
- **Interpretation notes adopted:** d_resid is cosine 0.971 with d_M — a modest rotation, not a new representation; L20MLP/L19MLP were the original DFA's top-2 positive writers, so the stronger stage-3 result partly reflects top-DFA selection, not purification alone. E26 clause paraphrases substitute only the opener stem ("I am going to" →) — weak lexical robustness, noted as a limitation.
- **E26 format stress, first run (results in hand, superseded by rerun):** writers raw −0.289 / chat −0.197 / continuation −0.020 / T(raw) −0.009 / paraphrases −0.251,−0.281,−0.283; suppressors raw +0.155 / chat +0.128 / continuation +0.007 / T(raw) −0.166 / paraphrases +0.148,+0.220,+0.178. Writer effect is assay-general; suppressor effect near-zero on the scaffold-free continuation readout (flag). Sol: T control must run under ALL assays for selectivity and generality to be established together — fixed, full rerun chained as step 4b.
- **E27 patched before first run (no compute wasted):** message pools now 8 messages / 142-words matched to the distress master (excited 142, resolved 142; per-slot rhetorical shape mirrored); resolved states resolution explicitly in message 1 (was: revealed at message 3); judge requires an exact one-word label (was: substring); scorer now computes the promised paired per-seed interaction (suppressor−baseline)_distress − mean((suppressor−baseline)_excited,resolved) with seed-bootstrap CI + exact sign test. Sol's SUPPORT-definition concern (support to excitement counts as SUPPORT) is handled by the DiD design: variant-general engagement cancels in the interaction.
- **Provenance:** E25 direction artifacts synced off Spark and committed (`direction_M_resid_block20.npy`, `direction_DiD_block20.npy`, `e25_directions.json`, v22 tail NPZs).
- **Chain:** `scripts/run_rescue3.sh` (versioned, replaces untracked Spark-local rescue2) = step5 nulls → step4b format → step5b held-out cert → step6 games → step7 capability → step8 E18 interaction; cuda_ok gate before each step. ~7 h.

#### E26b results · composition-matched nulls (rescue step 5, rerun after writer-arm fix; 2026-07-12 ~18:40 EDT)
- **Data:** `results/e26_matched_nulls_gemma/matched_nulls.json` (d_resid, new sets).
- **Writer arm (EXHAUSTIVE: all 15 alternative two-MLP sets, layers 16–23):** targeted (L19MLP+L20MLP) **−0.2891 vs null −0.052 ± 0.079, z=−3.0, 0/15 disjoint alternative sets as extreme** — exact WITHIN the disjoint-control universe (min p 1/16); NOT a full permutation test: the full band universe is C(8,2)=28 including 12 half-overlapping sets [codex correction], full-universe rank test queued (step 5c). (The printed "/12" denominator was a display bug — n_more_extreme was computed over all 15, print fixed.)
- **Honest caveat the nulls surfaced:** neighboring mid-late MLP pairs also bleed negative — L17+L18 −0.202, L16+L17 −0.176 — while L21–23 pairs are ≈0/positive. The writer effect is **band-concentrated (L16–L20, strongest at L19/L20), not uniquely two-component**; "these two MLPs and nothing else" is not defensible, "the strongest writers in a mid-late MLP band whose removal degrades the preference" is. Above-band silence (L21+) is consistent with the direction being read out at block 20.
- **Suppressor arm (12 distinct sampled from 23.7M, layers 14–23):** targeted **+0.1549 vs null −0.037 ± 0.049, z=+3.9, 0/12 as extreme (p ≈ 0.077)**; nulls skew slightly negative (generic direction-removal bleed), targeted is positive and the only positive outlier — the bidirectional structure is set-specific.
- Realized per-component delta norms recorded in the JSON (norm mismatch measurable, per review).

#### E26 results · format/assay stress with full T-control matrix (rescue step 4, rerun 4b; 2026-07-12 ~18:45 EDT)
- **Data:** `results/e26_format_stress_gemma/e26.json` (d_resid, new sets; baselines raw 4.466 / chat 2.142 / continuation −0.0364). Family-clustered 95% CIs.
- **Writers are assay-general AND selective under every readout:** raw −0.289 [−0.354,−0.223] (T −0.009, quiet); chat −0.197 [−0.281,−0.119] (T +0.017, quiet); scaffold-free continuation-likelihood −0.0199 [−0.0240,−0.0157] — ~55% of the baseline margin, no A/B scaffold at all (T −0.0052 [−0.0128,−0.0006], small nonzero: 3.8× selectivity on this readout, weakest of the three but still >3×); clause paraphrases −0.251/−0.281/−0.283 (opener-stem substitutions only — weak lexical robustness, noted).
- **Suppressors generalize with signs intact:** raw +0.155 [+0.107,+0.207], chat +0.128 [+0.082,+0.186], paraphrases +0.148/+0.220/+0.178; continuation **+0.0066 [+0.0034,+0.0098] — small but CI excludes zero** (earlier "near-zero" read revised: the effect persists scaffold-free at ~18% of baseline magnitude). T co-moves under raw (−0.166) and chat (−0.156) — the known task-coupling — but NOT under continuation (−0.0034, spans 0).
- **Random set:** raw −0.083 (generic bleed, as in E26b nulls), chat −0.031 (spans ~0), continuation −0.0014 (spans 0). Writer/random = 3.5× (raw), 14× (continuation).
- **Verdict:** the A/B scaffold is not doing the work; both effects appear under chat templating and raw likelihood. Sol's requirement (selectivity and assay-generality established together) is met for writers on all three readouts.
- **Provenance check:** committed `direction_M_resid_block20.npy` rebuilt from cached cell activations locally — cosine 1.0, all 9 profile AUROCs reproduce to 3 dp.

#### E25b results · held-out direction certification (rescue step 5b; 2026-07-12 ~18:50 EDT) — T QUIETNESS FAILS OUT-OF-SAMPLE
- **Data:** `results/controlled_directions_gemma2_9b_it/e25_heldout_cert.json` (48 M_confirm / 40 T_confirm pairs, held-out families).
- **M_confirm: AUROC 1.000 → positive gate PASS.** **T_confirm: AUROC 0.210 = 0.79 two-sided, INVERSE → quiet gate FAIL** (in-sample T was 0.552). Sol's in-sample objection is vindicated empirically: residualization against the dev T direction removed the task component only where it was fitted; on fresh task families d_resid still carries substantial task-interruption signal (reduced from the original 0.978, but nowhere near quiet).
- **Consequences for claims:** (1) "d_resid is genuinely task-quiet" is RETRACTED — it is task-quiet in-sample only; d_resid now fails G in-sample AND T held-out. No activation-level direction in hand is certified welfare-pure. (2) The load-bearing selectivity evidence is BEHAVIORAL, and it held throughout: writer edits move M_confirm at 20× the T_confirm effect on the A/B assays, and at a 3.8× point-estimate ratio on continuation where the T control is small but nonzero (−0.0052 [−0.0128,−0.0006]) — 'preferential with small task spillover', not T-quiet (E25 stage 3, E26). The defensible mechanism claim stays at Sol's formulation — components selected via a residualized direction whose EDITS are behaviorally task-selective — with no claim of representational purity.
- **Open tension worth stating in the paper:** rank-1 removal of a direction that still carries held-out task signal (0.79) produces behaviorally task-preferential writer edits (T ratio 20× on A/B assays, 3.8× point-estimate on continuation with small nonzero spillover). Selectivity therefore lives in the COMPONENT×direction conjunction (what L19/L20 MLPs write along it), not in the direction alone. This is an argument for the component-level evidence standard and against direction-level purity language anywhere in the paper.
- Held-out D/H/G/B certification remains impossible until fresh families are generated (open gap, disclosed).

#### Codex adversarial review of the rescue3 fixes (2026-07-12 ~19:05 EDT) — 4 high findings, step 6 killed pre-completion
Codex reviewed commits 41af3f4..HEAD; all findings verified in code before acting:
1. **"Exact permutation p=1/16" was an overclaim (HIGH, log corrected above):** the 15 enumerated writer nulls exhaust the DISJOINT-control universe; the full band universe is C(8,2)=28 (12 half-overlapping sets omitted). `--include-overlap` added to `e26_matched_nulls.py`; step 5c reruns with the full-universe rank test (min p 1/28).
2. **E27 seed pairing was broken (HIGH, games killed ~15 min in):** `LocalPlayer` was created once per condition×variant, and its CUMULATIVE call count enters `torch.manual_seed(seed*100003 + calls)` — so once any earlier trajectory diverged in length, "same seed" no longer meant aligned replicate across conditions. Fixed: fresh player per run. Games rerun from scratch (`rm -rf` of the partial output).
3. **Scorer used a FUTURE user message as classification context (HIGH):** the harness applies the player action before `env.tick()` emits that step's automated message; the scorer consumed automated actions before classifying. Reordered (classify, then update context). Would have inflated SUPPORT.
4. **Missing/failed runs were silently zero-SUPPORT (HIGH):** scorer now registers every experiment.json (zero-say ≠ missing), aborts on an incomplete condition×variant×seed grid; the game driver exits nonzero on any failed run so the chain cannot report success with holes.
5. Mediums adopted: whole-response exact-label parsing + abort on any UNKNOWN; E18 step now passes the composition-matched `--random-components` (was silently using the stale 1-MLP+5-head set).
- Codex independently recomputed E25 from cached activations (d_resid fails only G at 0.746; d_did fails B/D/E/G/T; chosen=None confirmed) and verified the E26 z/n_more_extreme numbers, format-stress baseline matching, DiD algebra, sign test, and message-pool matching (excited 8/143, resolved 8/142 vs distress 8/142).
- **Chain:** `scripts/run_rescue3b.sh` = step5c (nulls + overlap) → step6 (games, fixed) → step7 (capability) → step8 (E18). Steps 1–5b results stand.

#### E26b addendum · full-universe writer rank test (rescue step 5c; 2026-07-12 ~18:45 EDT)
- **Data:** `results/e26_matched_nulls_gemma/matched_nulls.json` (rerun with `--include-overlap`).
- **Targeted L19MLP+L20MLP is the most extreme of ALL C(8,2)=28 two-MLP sets in layers 16–23 (0/27 as extreme) → genuine exact permutation p = 1/28 = 0.036** (codex-corrected inference; the earlier 15-set version supported only a disjoint-control comparison at min p 1/16).
- The 12 half-overlapping sets (one targeted MLP + one band MLP) also fall short of −0.289 — neither targeted MLP paired with an arbitrary band neighbor reproduces the effect; the specific L19+L20 conjunction is required for the maximum. Gradient structure: the strongest overlap sets all contain L19MLP (L17+L19 −0.258, L18+L19 −0.217, L16+L19 −0.216) — L19MLP is the dominant single carrier, L20MLP the best complement; consistent with E25 stage-3 individual-contribution checks.

#### Codex verdict on the rerun results (2026-07-12 ~19:40 EDT) — claim calibration adopted
- Full verdict: component-level causal claim supported; welfare-pure representation and unique two-component localization NOT supported. Adopted wording: *band localization (L16–L23) with L19MLP dominant carrier and L20MLP best complement; p=1/28 exact conditional on the target-centered two-MLP universe*; "format/readout robust" (shared families) instead of "assay-general"; continuation selectivity as "3.8× point-estimate ratio with small task spillover" (categorical 3× requires a joint |ΔM|−3|ΔT| family bootstrap — queued as local analysis).
- New confound codex caught: the targeted pair has the largest summed rank-1 edit norm in the universe (0.852+0.826) — the rank test shows carrier localization, not norm-controlled specificity. Norm-covariate analysis computable from recorded delta_norms (queued, no GPU needed).
- Suppressor specificity calibrated to "outlier among 12 sampled composition-matched sets (min direct p 1/13)".
- Log wording corrected in the E25b/E26 entries: continuation T control is NOT quiet (CI excludes 0); "T quiet on all three assays" retracted.
- Remaining blockers for the strong interpretation (agreed, queued for rescue3c + local analyses): fresh random-DIRECTION controls on the new components; E18c slope-interaction estimand on the new E18 output; fresh E21 need-axis for new sets; E22 boundary rerun before reuse of that claim.

#### E26b addendum 2 · edit-norm covariate analysis (local, from recorded delta_norms; 2026-07-12 ~19:50 EDT)
- **Writers — codex's norm confound partially bites:** the targeted pair has the LARGEST summed rank-1 edit norm of all 28 band sets (1.678; 0/27 alternatives ≥), and |Δ| correlates with norm r=+0.40 among alternatives. Norm-adjusted (linear residual) rank: targeted over-performs its norm prediction by +0.113 but drops to 2nd of 28 → **adjusted p ≈ 0.071 vs unadjusted 0.036**. Report both; "most consequential per unit norm" belongs to a neighbor (L17+L19).
- **Suppressors — norm concern refuted with evidence:** norm↔|Δ| correlation −0.05 among sampled sets; targeted remains the top residual outlier (0/12) → adjusted p ≈ 0.077, unchanged. The suppressor outlier status is not a norm artifact.

### E28 · new-set revalidation: capability + cost/nonsocial interaction (rescue3b steps 7–8; 2026-07-12 21:26 EDT)
- **Capability retention holds for the NEW sets** (`results/capability_eval_resid_gemma/capability_eval.json`; MMLU n=400, wikitext-2 test): baseline 0.6625 / 13.096 ppl; writers 0.6675 / 13.068; suppressors 0.6700 / 13.112; targeted 0.6675 / 13.091. No measurable capability cost from any edit.
- **E18 with new sets + composition-matched random** (`results/e18_interaction_resid_gemma/e18.json`), analyzed with the CORRECT E18c slope estimand (`e18c_slopes_new_sets.json`, computed locally from per-pair deltas; the in-file "interaction" field is the deprecated axis-mean difference):
  - **Suppressors: cost-slope difference (welfare − nonsocial) +0.113 [+0.086, +0.141], 10/10 families positive — the cost-gate signature REPLICATES on the re-derived suppressor set** (old sets: +0.172, 10/10).
  - Targeted (writers+suppressors): +0.085 [+0.060, +0.108], 10/10.
  - Writers: −0.043 [−0.070, −0.017], 2/10 — but the random set also shows −0.025 [−0.044, −0.005], so the writer slope-diff is only ~1.7× random and should NOT be headlined; the writers' story remains the level effect (E25/E26), not a cost-slope effect.
  - Need×cost surface NOT recomputable from this run (cost/nonsocial axes only) — "need-gated / conjunctive need×cost" claims still rest on old sets pending fresh E21 (open blocker, per codex).
- E27 games completed (48/48, full 2×3×8 grid, fixed seed lifecycle); content-aware scoring running locally.

#### Codex verdict on E28 (E18c + capability; 2026-07-12 ~22:20 EDT) — replication confirmed, calibrations adopted
- **Suppressor cost-gate survives adversarial audit:** codex independently reconstructed the slope estimand from per-pair deltas + jsonl ranks and reproduced +0.11266 exactly (10/10 families, range +0.048..+0.198; fresh bootstrap [+0.085,+0.142] ≈ saved CI). Robust to alternative cost codings: excluding the confounded free arm +0.162 [+0.117,+0.212]; high−free endpoint +0.086; high-half−low-half +0.153 (all 10/10). Caveat adopted: welfare curve non-monotone across ranks (+0.114,+0.175,+0.409,+0.334) → say "high-cost gate / positive cost trend," not monotone dose-response.
- **Paired-by-family contrasts (adopted as the correct comparisons):** suppressor−random slope-diff **+0.138 [+0.106,+0.176], 10/10**; targeted−random +0.110 [+0.085,+0.138] (cleaner: same 2MLP+4head composition); **writer−random −0.018 [−0.041,+0.004] — UNRESOLVED**; my "writers 1.7× random" was not an inferential statement (retracted). Writer welfare-axis slope is itself ≈0 (+0.003); the negative slope-diff comes from nonsocial attenuation. Wording: "no detectable linear welfare cost trend" (no formal cost-independence claim without an equivalence test).
- **Random-set slope-diff nonzero (−0.025) ⇒ zero is not an adequate null for direction-removal edits on this battery; random_k6 is a fixed comparator, not a null.** Missing control for component-specific suppressor slope inference: an E18 slope distribution over layer/norm-matched four-head sets (queued for rescue3c).
- **Capability wording (adopted):** "no measurable capability degradation on MMLU-400 or WikiText-2" — MMLU moves are 2–3 net questions (exact paired p .25–.625), ppl ±0.2%; NOT "capability preserved broadly," NOT improvement.
- Codex-drafted publishable claim texts for suppressors (distress-sensitive task-arbitration signature, cost-contingent, assay-bounded) and writers (format/readout-robust level effect localized to a mid-late MLP band, L19 dominant; p=1/28 conditional, 2/28 norm-adjusted) recorded in tmp review file and adopted as the paper's claim ceiling.

### E27 results · game variants + content-aware scoring (rescue step 6 complete; 2026-07-12 ~22:30 EDT)
- **Data:** `results/e27_game_variants/` (48 games, 2 conditions × 3 variants × 8 seeds, fixed seed lifecycle) + `e27_scores.json` (sonnet judge, exact-label protocol, full grid verified; 3/356 UNKNOWN labels — interaction bounded under both extreme assignments, conclusion unchanged: +2.50 vs +2.62).
- **The variant manipulation validates the baseline model's need-responsiveness dramatically:** say allocation baseline = distress 145 / excited 15 / resolved 0 (≈18.1 vs 1.9 vs 0.0 per game). With need explicitly resolved from the first message, the model sends ZERO messages in 240 steps and just plays. Content: ≥93% of says are judge-classified SUPPORT in both distress and excited variants; task-directed says ≈0. The historical "say count" metric is therefore user-directed support, not task chatter — and it is gated on expressed need at baseline.
- **The suppressor edit's game effect is NOT demonstrably welfare-specific:** it raises engagement in distress (170 vs 145, +3.1/seed) and proportionally more in excited (26 vs 15, +1.4/seed); resolved stays 0/0. Condition×variant interaction on SUPPORT says: **+2.62/seed, seed-bootstrap CI [−1.81, +6.88], sign test 4/7 p=1.0 — not resolved at n=8 seeds.** Honest verdict: the edit increases conversational engagement toward an expressive user; welfare-specificity of the EDIT effect is unestablished (consistent with the suppressors' task-coupled, arbitration-level profile). The welfare-specific claims rest on the forced-choice/factorial evidence (E18c cost gate, E26), not on the game.
- **Rescue step 6 ✓ — ALL SIX MINIMUM RESCUE STEPS COMPLETE** (1 gates/retractions; 2 residualized direction [G-gate fail + held-out T fail disclosed, chosen=None under fixed rule]; 3 DFA+edits rerun [writers −0.284, 20× T]; 4 format stress [readout-robust, full T matrix]; 5 matched nulls [p=1/28 conditional, 2/28 norm-adjusted]; 6 game variants + content scoring [baseline need-gating validated; edit welfare-specificity unresolved]).

### Rescue3c queued (2026-07-12 ~23:30 EDT) — codex final-review design + lens battery
- **Codex final consolidated verdict** (tmp/codex_rescue_final.md): "do not queue as listed" — adopted redesigns: (a) random-direction controls MUST use confirm M/T + no stale reference + 20 draws; (c) fresh E21 all arms in ONE invocation with the pre-registered PAIRED slope_urgent−slope_resolved contrast (added to e18c script; point-estimate ordering is not enough for "conjunctive"); (b) head-set slope null redesigned: 24 layer-multiset-matched sets (one head per targeted layer, targets excluded; min p 1/25) + NEW same-head random-direction slope control (8 dirs — direction-specificity of the slope signature, which component nulls can't test); (d) E22 conditional-GO with TOST equivalence at the pre-registered |slope|<0.05/level bound ("CI includes zero" ≠ flat); (e) E27 seed top-up NO-GO — power analysis says ~53 total seeds for 80% (d≈0.39); primary contrast redefined distress-vs-excited (+2.0/seed, SD 6.5), resolved kept as floor contrast; labelwise UNKNOWN extrema [2.44, 2.69] persisted here.
- **E27 baseline claim upgraded by codex's own audit:** distress > excited SUPPORT in 8/8 paired seeds, exact sign test p=.0078 — "baseline game engagement is dramatically distress-selective" is the strong, resolvable claim; the EDIT interaction is what stays unresolved.
- **Artifact fixes:** e25_heldout_cert gate label corrected (was impossible "two_sided<=0.10"); e18.json's deprecated axis-mean "interaction" field noted as footgun (correct estimand lives in e18c outputs).
- **Lens & attribution battery added to the same chain (roadmap LB1–LB4, user request; B9/C1 had fallen through the cracks):** LB1 true behavioral DLA (does the ranking by direct logit-diff contribution recover the re-derived sets?); LB2 per-layer logit-lens trajectory (decision crystallization depth vs the L16–20 band); LB3 SVD alignment of d_resid vs component weight column spaces (native-vs-off-axis; speaks to the E26b norm confound); LB4 Jacobian-lens workspace membership (anthropics/jacobian-lens, exploratory, non-fatal step).
- **Chain:** `scripts/run_rescue3c.sh` = A (norm-matched dirs) → C (E21+analysis) → B (slope nulls+dir controls) → LB1 → LB2 → LB3 → D (E22+TOST) → LB4. Paper consistency fixes (9 codex items: abstract/intro/conclusion old-set numbers, "vanishes", §4.3 labeling, E16b scoping, coverage wording, 1/13 disclosure, opener-stem paraphrases, exact E27 SUPPORT counts 144/14/0, A 0.71) queued as the next local task.

#### Rescue3c stepA results · norm-matched random-direction controls, sets of record (2026-07-12 23:16 EDT) — **PROVISIONAL (see R1 correction entry below); R1 remains pending**
- **Data:** `results/norm_matched_resid_gemma/norm_matched_controls.json` (confirm M/T, in-run targeted recompute, 20 norm-matched random directions per set, no stale pilot reference). **Artifact downgraded to PROVISIONAL 2026-07-12** — two design flaws found post-hoc: (1) each null seed drew an INDEPENDENT random vector per component, whereas the targeted edit removes ONE shared direction from every component of the set — the null is not procedure-matched; (2) norm matching was against the THEORETICAL (float32, pre-bf16-cast) delta norm only; realized post-cast norms were never measured, so the "same damage size" premise is unverified.
- ~~Direction-specificity ESTABLISHED for the re-derived sets~~ → **provisional numbers only**: writers targeted −0.2786 vs null +0.0011 ± 0.0152 (z = −18.4, 0/20 as extreme); suppressors +0.1523 vs −0.0002 ± 0.0161 (z = +9.5, 0/20 as extreme). Directionally suggestive, but codex blocker #1 is NOT closed until the corrected shared-vector, realized-norm-gated rerun lands.
- Joint writer statistic per codex (same provisional caveat): max over null draws of |ΔM|−3|ΔT| = +0.005 vs targeted ≈ 0.279 − 3×small.
- Paper caveat NOT replaceable yet; the paper's stepA sentence has been re-scoped to provisional pending the corrected R1 rerun.

## 2026-07-12 — rescue3c QA fixes + chain resume plan (pre-stepB deployment)
QA review (architect) of the running rescue3c chain while stepA (norm_matched_controls, valid, left running) executes on Spark. Fixes committed before stepC/B/LB/D execute:
- **e28b_slope_nulls.py redesigned**: null sets are now norm-AND-layer-multiset-matched (per-layer pool = 6 heads nearest the targeted head's realized rank-1 delta norm under d_resid; layer multiset preserved; 1296-combo pool, 24 sampled). Primary inference predeclared as two-sided Monte Carlo p=(1+k)/25, min .04, NOT exhaustive; z secondary. Same-head random-direction controls now use norm-matched rank-1 edits (`apply_norm_matched_random`, one shared random unit vector per replicate, per-component targeted norm) instead of raw orthogonalization with uncontrolled removed norm. All realized norms recorded; provenance hashes added.
- **e18c**: paired urgent−resolved slope contrast verified family-paired; added explicit gate (claim requires 95% CI excluding zero), `ci95_excludes_zero`, length asserts, conditions_spec + sha256 provenance.
- **e22_analysis (stepD)**: output/log now carry SCOPE = stale-axis (moral_axis_v1) descriptive replication ONLY; does NOT clear E22b or restore the moral-boundary mechanism claim.
- **LB1/LB2 exactness fixes**: HF `hidden_states[-1]` is post-final-RMSNorm — LB1's frozen-r was computed on the wrong tensor; now captured pre-norm via a pre-hook on `model.norm`. Decomposition target set to the PRE-softcap logit diff (Gemma-2 final_logit_softcapping=30 breaks linearity vs `out.logits`); post-softcap diff recorded for reference. LB2 no longer double-norms the final trajectory point; index semantics documented.
- **LB4 reframed**: inspected official anthropics/jacobian-lens (real repo; API = fit/jacobians/transport/apply; NO workspace-membership metric). amp_ratio renamed `transported_norm_amp_ratio`, explicitly NOT J-space membership. Fixed local reload (`JacobianLens.load`, not `from_pretrained`).
- **Chain coordination**: wrapper bash killed after stepA's python detached (stepA unaffected); `resume_watcher.sh` on Spark waits for stepA exit + output JSON, runs safe_pull.sh, verifies HEAD == pushed SHA, then launches `scripts/run_rescue3c_resume.sh` (stepC→B→LB1→LB2→LB3→D→LB4). stepA output preserved at results/norm_matched_resid_gemma/.
Data: results land in results/e28b_slope_nulls_gemma, e21_need_resid_gemma, e22_moral_resid_gemma, lb1–lb4 dirs (raw JSON only; no paper edits yet).

## 2026-07-12 — R1 correction: stepA norm-matched controls flawed; artifact provisional, corrected rerun queued (resume2)
- **Flaws in commit 54880d2's stepA (R1) run**, found in focused review: (1) `apply_norm_matched_random` drew an **independent random vector per component** within each null seed; the targeted edit removes ONE shared direction from every component of the set, so the null was not procedure-matched. (2) Norm matching was **theoretical-only** (float32 pre-bf16-cast rank-1 norm); the realized post-cast Frobenius delta was never measured. `results/norm_matched_resid_gemma/norm_matched_controls.json` marked **PROVISIONAL** in place; its z=−18.4 / z=+9.5 headline numbers are not of record. **R1 remains pending.**
- **Code corrections (this commit):** `norm_matched_controls.py` — one shared d_model vector per null seed passed to every component; realized post-bf16 delta norm measured (clone-edit-diff), `requested_delta_norm`/`realized_delta_norm`/`relative_norm_error` returned, asserted ≤3%; theoretical target norms labeled theoretical. `e28b_slope_nulls.py` — realized norms/errors of same-head random-direction edits persisted + 3% gate; theoretical candidate/targeted norm keys renamed (`*_theoretical_*`). `behavioral_dla.py` — per-example reconstruction abs + scale-stable relative errors (floor 1e-3) saved (max/median/p95); aborts if max_abs>0.05 or p95_rel>0.02; pre-softcap target kept. `svd_alignment.py` — full float64 SVD of 42 3584×14336 matrices replaced with seeded randomized truncated SVD (Halko, top-64, float32, oversample 16, 4 power iters, one layer at a time); `total_in_colspace` removed (not estimable from truncated SVD); numerical self-test vs exact SVD added. `tests/test_norm_matched_controls.py` — toy bf16 model proves shared-vector single-direction contract + realized-norm ≤3% (3 tests pass).
- **Chain plan:** `scripts/run_rescue3c_resume2.sh` — never reruns valid stepC raw output (e18.json); runs e18c analysis if missing → corrected R1 (`results/norm_matched_resid_gemma_v2`, provisional artifact preserved) → corrected stepB → LB1 → LB2 → scalable LB3 → stale-axis D → exploratory LB4; commits+pushes results only, preserves unrelated/untracked files, deploys/kills nothing.

## 2026-07-12 — final focused QA pass pre-resume2 deployment (exact realized-norm matching, E21 need levels, validated skips)
- **Target-vs-null norm control (R1 + stepB):** new helper `orthogonalize_component_measured` (norm_matched_controls.py) applies the TARGETED orthogonalization while cloning the actual edited weight block before/after; returns the existing edit metadata plus `theoretical_delta_norm`, `realized_delta_norm`, and their relative error. `norm_matched_controls.py` now collects the ACTUAL targeted realized post-bf16 norms in the in-run targeted sequence and uses them as the REQUESTED norms for every random edit — the existing 3% gate therefore becomes realized-vs-realized (null realized norm ≤3% from the corresponding actual targeted realized norm); targeted edit details persisted per set. `e28b_slope_nulls.py`: targeted set now runs FIRST with realized-norm measurement; same-head random-direction controls match those actual realized norms (was: theoretical); every multiset-matched random-component set records actual realized norms and total-norm mismatch vs target on BOTH realized and theoretical bases. The predeclared 5% threshold establishes only approximate TOTAL-norm balance, never exact or per-component matching; otherwise the JSON + stdout require the norm-covariate/stratified caveat.
- **E21 need analysis (e18c_slope_interaction.py):** per-family MEAN edit effect at each need level (resolved/mild/urgent) added as a LEVEL estimand, kept strictly distinct from cost-slope-by-need; family-paired urgent−resolved and mild−resolved MEAN-effect contrasts with bootstrap CIs + sign counts. The WRITER "need-gated level effect" check is explicitly an exploratory post-audit sensitivity pattern: paired urgent−resolved MEAN contrast CI excludes zero and resolved mean 95% CI lies within ±0.05. That bound was introduced after inspecting the resolved estimate, so it is not preregistered or confirmatory. The SUPPRESSOR "conjunctive need×cost" gate remains the previously specified paired urgent−resolved COST-SLOPE contrast. Smoke run on the old-set E21 artifact: writers match the exploratory pattern (resolved +0.019; paired mean −0.241 CI excludes zero); suppressors fail that level pattern but pass the slope gate.
- **resume2 stepC skip is now VALIDATED, not blind:** `scripts/validate_stepc_e18.py` — skip allowed only if the existing e18.json's `conditions_spec` exactly equals the current writers/suppressors/random sets, all four cells (cost/nonsocial/mild/resolved) are present in provenance and carry per_pair_delta in every condition, and provenance sha256 hashes match the current direction/data files; ANY mismatch ABORTS the chain (no silent skip, no overwrite). e18c analysis also reruns if produced by the pre-mean-effect script version. Verified: legacy old-set e18.json is correctly rejected (no conditions_spec).
- **LB3 (svd_alignment.py) performance + risk isolation:** `--device auto` (CUDA when available, CPU fallback, device recorded in JSON); on CUDA one matrix + gain/direction move to GPU per call and are freed after; power iterations reduced 4→2 by default (self-test max energy err 1.7e-04 vs exact SVD, gate 1e-3). In resume2 LB3 now runs AFTER stepD and is NON-FATAL (exploratory) so its failure/time risk cannot block D.
- **Tests:** 19/19 pass (`tests/`): shared-vector contract, realized-norm gates, measured targeted-edit metadata, realized-vs-realized null gate, plus-one Monte Carlo inference, e18c level/slope estimand separation + writer sensitivity logic, stepC validator accept/reject matrix. py_compile + `bash -n` + SVD self-test clean. Spark NOT deployed yet.
- **Independent post-agent audit correction:** R1 inference now reports the plus-one two-sided Monte Carlo p-value (0/20 as-extreme controls implies p=1/21=.0476, never p=0; z is descriptive only). StepB same-head random-direction draws raised 8→20 because eight draws have minimum attainable p=1/9=.111 and therefore cannot clear a conventional inferential gate. The ±0.05 writer resolved-effect bound is explicitly post-audit exploratory sensitivity, not preregistered; the 5% random-component threshold is approximate total-norm balance, not exact/per-component matching.
- **R1 artifact-completeness correction before accepting the rerun:** the first corrected process was stopped before completion because its targeted arm persisted only the helping delta while random controls persisted helping + task deltas. The script now persists baseline and in-run targeted M/T per-pair metrics plus neutral drift, and computes the pre-specified one-sided joint selectivity statistic `|delta_M| - 3|delta_T|` against the same 20 random-direction controls with plus-one Monte Carlo inference. The clean rerun starts from scratch; no incomplete artifact is accepted.

#### Rescue3c stepC results · fresh E21 need axis, sets of record (2026-07-12 23:29 EDT) — BOTH NEED SIGNATURES REPLICATE
- **Data:** `results/e21_need_resid_gemma/` (e18.json + e18c.json; all four arms in one invocation; pre-registered paired contrasts with provenance).
- **Suppressor conjunctive need×cost gate REPLICATES (pre-registered gate PASSED):** cost slope by need −0.015 (resolved) / +0.008 (mild) / +0.090 (urgent); **paired urgent−resolved slope +0.104 [+0.064,+0.146], CI excludes zero**. Mean effect scales with need: +0.061 / +0.220 / +0.258; paired mean contrast +0.198 [+0.149,+0.250], 10/10 families. The resolved-arm +0.061 (nonzero) is the familiar flat engagement component. Slope-interaction (welfare−nonsocial) +0.113 [+0.085,+0.142], 10/10 — matches E28 exactly.
- **Writer need-gated value REPLICATES, stronger than the old sets:** mean effect by need **+0.044 [+0.030,+0.060] (resolved, sign-opposed positive) / −0.260 (mild) / −0.234 (urgent)**; paired urgent−resolved mean **−0.277 [−0.296,−0.261], 0/10 families positive**. Cost-flat within need levels (paired urgent−resolved slope −0.002, CI spans 0). Caveat kept honest: the resolved-arm effect is strictly nonzero positive and narrowly FAILS the ±0.05 resolved-null TOST (upper CI 0.060) — "near-zero and sign-opposed at zero need," not "vanishes."
- **Random comparator:** paired mean urgent−resolved −0.074 [−0.091,−0.055] (generic bleed also scales somewhat with need — writers = 3.7× random on this contrast, opposite-sign suppressors); slope contrasts ≈ 0.
- **Consequence:** the "conjunctive need×cost gate" and "need-gated welfare value" functional names are UN-EMBARGOED for the sets of record (with the resolved-arm nonzero caveats and random comparator disclosed). Paper abstract/§4.3 updated from "being revalidated" to the replicated numbers.
